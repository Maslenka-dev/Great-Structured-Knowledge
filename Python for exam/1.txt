На рисунке схема дорог Н-ского района изображена в виде графа, в таблице содержатся сведения о протяжённости каждой из этих дорог (в километрах).
Так как таблицу и схему рисовали независимо друг от друга, то нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите, какова сумма протяжённостей дорог из пункта H в пункт D и из пункта A в пункт B. В ответе запишите целое число.

	from itertools import permutations
	s = '68 47 45 237 368 15 248 157'.split()
	v = 'CE EG GF GF FA AC CD DH HE FB AB'.split()
	print(*range(1,9))
	for p in permutations('ABCDEFGH'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a, b in v):
	        print(*p)

Теперь Снегурочке нужно узнать, какие номера в таблице соответствуют городам K и Q. Помогите дочери Деда Мороза и определите правильные номера этих населённых пунктов. В ответе запишите два номера в возрастающем порядке без пробелов и знаков препинания.

	from itertools import permutations
	s = '3457 456 167 128 126 2358 138 467'.split()
	v = 'KB BP PL LK KR BC AQ LA LR RC CQ AQ AC'.split()
	print(*range(1, 9))
	for p in permutations('LKBPAQRC'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a, b in v):
	        print(*p)

На рисунке справа схема дорог Н-ского района изображена в виде графа, а в таблице слева указаны длины каждой из этих дорог. Так как таблицу и схему рисовали независимо друг от друга, неизвестно, каким номерам населённых пунктов в таблице соответствуют буквенные обозначения на графе.

	from itertools import permutations
	s = '235 145 146 2367 1267 345 45'.split()
	v = 'ED DA AF FE EC DG AG FB FC CG GB'.split()
	print(*range(1, 8))
	for p in permutations('ABCDEFG'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a, b in v):
	        print(*p)

На рисунке схема дорог между населёнными пунктами изображена в виде графа, а в таблице приведены длины этих дорог. Так как таблицу и схему рисовали независимо друг от друга, неизвестно, каким номерам населённых пунктов в таблице соответствуют буквенные обозначения на графе.

	from itertools import permutations 
	s = '49 4569 789 128 268 258 389 34567 1237'.split()
	v = 'ЗЕ ЕЖ ЖБ БД ДЗ ДА ДВ БВ ЖВ ЖИ ЖГ ЕА АИ АГ ИГ'.split()
	print(*range(1, 10))
	for p in permutations('АБВГДЕЖЗИ'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a, b in v):
	        print(*p)

На рисунке изображена схема дорог N-ского района. В таблице звёздочкой обозначено наличие дороги из одного населённого пункта в другой. Отсутствие звёздочки означает, что такой дороги нет.
Каждому населённому пункту на схеме соответствует номер в таблице, но неизвестно, какой именно номер. Определите, какие номера в таблице могут соответствовать населённым пунктам D и Е на схеме.
В ответе запишите эти два номера в возрастающем порядке без пробелов и знаков препинания.

	from itertools import permutations
	s = '345 467 14 123567 147 24 245'.split()
	v = 'AF FE ED DC CB AG FG EG DG CG BG'.split()
	print(*range(1, 8))
	for p in permutations('ABCDEFG'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a, b in v):
	        print(*p)

На рисунке схема дорог N-ского района изображена в виде графа, в таблице содержатся сведения о протяжённости каждой из этих дорог (в километрах).Так как таблицу и схему рисовали независимо друг от друга, то нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите, какова сумма протяжённостей дорог из пункта А в пункт Б и из пункта Д в пункт Е.
В ответе запишите целое число.

	from itertools import permutations
	s = '56 134 257 267 37 14 345'.split()
	v = 'АБ БВ ВГ ГК КЕ ЕД ДА БЕ ВК'.split()
	print(*range(1, 8))
	for p in permutations('АБВГДЕК'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a,b in v):
	        print(*p)

На рисунке схема дорог N-ского района изображена в виде графа, в таблице содержатся сведения о протяжённости каждой из этих дорог (в километрах). Так как таблицу и схему рисовали независимо друг от друга, то нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите, какова сумма протяжённостей дорог из пункта В в пункт Г и из пункта Д в пункт Е.
В ответе запишите целое число.

	from itertools import permutations
	s = '23 135 124567 36 237 347 356'.split()
	v = 'АК КЕ ЕД ДГ ГВ ВБ АБ АВ АГ АД АЕ АК'.split()
	print(*range(1, 8))
	for p in permutations('АБВГДЕК'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a,b in v):
	        print(*p)

На рисунке схема дорог N-ского района изображена в виде графа, в таблице содержатся сведения о протяжённости каждой из этих дорог (в километрах).
Так как таблицу и схему рисовали независимо друг от друга, нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите, какова сумма протяжённостей дорог из пункта G в пункт E и из пункта F в пункт H.
В ответе запишите целое число.

	from itertools import permutations
	s = '258 17 56 68 138 347 26 145'.split()
	v = 'DA AC CB BH HD HF FE EG GA GC'.split()
	print(*range(1, 9))
	for p in permutations('ABCDEFGH'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a, b in v):
	        print(*p)

На рисунке схема дорог Н-ского района изображена в виде графа, в таблице содержатся сведения о длине этих дорог в километрах. Так как таблицу и схему рисовали независимо друг от друга, нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите сумму длин дорог ЕЗ и ДЖ. В ответе запишите целое число.

	from itertools import permutations
	s = '235 13 1245678 36 13 347 368 37'.split()
	v = 'АБ БГ ГЕ ЕЗ ЗЖ ЖД ДГ ГВ ВА АГ ГЗ ГЖ'.split()
	print(*range(1, 9))
	for p in permutations('АБВГДЕЖЗ'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a,b in v):
	        print(*p)

На рисунке справа схема дорог Н-ского района изображена в виде графа, в таблице содержатся сведения о длинах этих дорог (в километрах). Так как таблицу и схему рисовали независимо друг от друга, то нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите минимально возможную длину пути BDE. Передвигаться можно только по указанным дорогам.

	from itertools import *
	s = '47 57 45 136 236 457 126'.split()
	v = 'AG GF FB BD DE EA GC CB CE'.split()
	print(*range(1, 8))
	for p in permutations('ABCDEFG'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a,b in v):
	        print(*p)

На рисунке схема дорог N-ского района изображена в виде графа, в таблице содержатся сведения о протяжённости каждой из этих дорог (в километрах).
Так как таблицу и схему рисовали независимо друг от друга, нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите, какова сумма протяжённостей дорог из пункта С в пункт В и из пункта А в пункт Н.
В ответе запишите целое число.

	from itertools import *
	s = '346 348 12 127 678 15 458 257'.split()
	v = 'DC CF FG GE EA AD DB CB GH AH BH'.split()
	print(*range(1, 9))
	for p in permutations('ABCDEFGH'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a,b in v):
	        print(*p)

На рисунке схема дорог N-ского района изображена в виде графа, в таблице содержатся сведения о протяжённости каждой из этих дорог (в километрах). Так как таблицу и схему рисовали независимо друг от друга, нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите, какова сумма протяжённостей дорог из пункта Е в пункт D и из пункта G в пункт D. 
В ответе запишите целое число.

	from itertools import *
	v = 'DG GA AH HF FE FB HB GC BC'.split()
	s = '478 38 256 15 34 37 168 127'.split()
	print(*range(1, 9))
	for p in permutations('ABCDEFGH'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a,b in v):
	        print(*p)

На рисунке схема дорог N-ского района изображена в виде графа, в таблице содержатся сведения о протяжённости каждой из этих дорог (в километрах). Так как таблицу и схему рисовали независимо друг от друга, то нумерация населённых пунктов в таблице никак не связана с буквенными обозначениями на графе. Определите, какова сумма протяжённостей дорог из пункта C в пункт D и из пункта A в пункт F. В ответе запишите целое число. 

	from itertools import *
	s = '457 567 45 136 123 247 126'.split()
	v = 'AB BG GE EF AF FD AD BC EC'.split()
	print(*range(1, 8))
	for p in permutations('ABCDEFG'):
	    if all(str(p.index(b) + 1) in s[p.index(a)] for a,b in v):
	        print(*p)