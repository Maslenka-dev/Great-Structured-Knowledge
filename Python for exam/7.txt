s = 16 * 1000
l = 16
t = 80
k = 1
print((s * l * t * k) / 8 / 1024)


a = 2
s = 48 * 1000
f = 24
l = 5625 * 1024 * 1024 * 8
print((l / s / f/ a) / 60)


a = 2
l = 44 * 1000
n = 5 * 60 + 25
v = 82 * 1024 * 1024 * 8
print(v / n / l / a)


a = 2
s = 6
t = 3 * 60 + 15
v = 12 * 1024 * 1024 * 8
print(v / t / s / a)


print((48 * 0.5 * 1.5) / 3)


print(((16875 * 1024 * 8) / 1 / 16 / 48000) / 60)


t = 5 * 60
s = 4
a = 40 * 1000
l = 16
print((t * s * a * l) / 1024 / 1024 / 8)


При создании четырехканальной (квадро) звукозаписи использовалось 256 уровней громкости. Каждый отсчёт кодировался минимально возможным и одинаковым количеством бит. Запись длилась 8 минут 32 секунды. В результате получился файл, объём которого без учёта заголовка составил 62,5 Мбайт.
С какой частотой дискретизации, выраженной в Герцах, производилась запись.

	from math import log2
	a = 4
	n = log2(256)
	t = 8 * 60 + 32
	v = 62.5 * 1024 * 1024 * 8
	print(v / t / n / a)


Производится двухканальная звукозапись с частотой дискретизации 16 кГц. Запись длится 4 минуты 16 секунд, её результаты записываются в файл. Сжатие данных не используется. Каждый отсчёт кодируется минимально возможным и одинаковым количеством бит. Информационный объём полученного файла без учета заголовка не превышает 16 Мбайт.
Определите максимальное количество бит, которое могло использоваться для кодирования одного отсчёта.

	a = 2
	s = 16 * 1000
	t = 4 * 60 + 16
	v = 16 * 1024 * 1024 * 8
	print(v / t / s / a)


Производилась двухканальная (стерео) звукозапись с частотой дискретизации 44100 Гц и 32-битным разрешением. В результате получен файл размером 50 Мбайт без учёта размера заголовка и без сжатия данных.
Определите длительность звукозаписи (в минутах). В качестве ответа укажите ближайшее к полученному времени записи целое число.

	a = 2
	l = 44100
	n = 32
	v = 50 * 1024 * 1024 * 8
	print((v / n / l / a) / 60)


Для быстрой передачи звукового файла размером 210 Мбайт, записанного в формате стерео, его предварительно сжимают. Перед сжатием исходный файл преобразуют в моно-формат и конвертируют, при этом уменьшая частоту дискретизации вдвое и увеличивая глубину кодирования в 1.5 раза. После чего получившийся файл сжимают. Исходный файл больше сжатого на 40%.
За сколько секунд будет передан сжатый файл, если средняя пропускная способность канала составляет 5040 бит/сек? В ответе укажите целую часть полученного значения.

	x = (210 * 1024 * 1024 * 8 * 0.5 * 0.5 * 1.5) / 1.4
	print(x / 5040)


Музыкальный фрагмент записали в формате стерео (двухканальная запись), оцифрован и сохранён в виде файла без использования сжатия данных. Размер полученного файла без учёта размера заголовка файла — 48 Мбайт. Затем тот же музыкальный фрагмент был записан повторно в формате моно и оцифрован с разрешением в 1.5 раза выше и частотой дискретизации в 3 раза меньше, чем в первый раз. Размер полученного файла без учёта размера заголовка файла — 6 Мбайт. При повторной оцифровке использовалось сжатие. Определите коэффициент сжатия (отношение размеров несжатого и сжатого файла).

	l = (48 * 1.5) / (2 * 3)
	print(l / 6)


СЛОЖНАЯ!!
Музыкальный фрагмент был записан в формате стерео (двухканальная запись), частотой дискретизации 48 кГц и 16-битным разрешением. При сохранении файла использовали сжатие, позволившее уменьшить размер на 20%. Размер полученного файла — 32 Мбайт. В целях экономии памяти музыкальный фрагмент было решено записать повторно в формате моно, частотой дискретизации 24 кГц и 16-битным разрешением. При сохранении файла использовали сжатие, после которого размер уменьшился на 40%.
Укажите размер полученного при повторной записи файла в Мбайт. В ответе запишите только число, единицу измерения писать не нужно.

	l = 32 / 0.8
	n = (l / 4) * 0.6
	print(n)