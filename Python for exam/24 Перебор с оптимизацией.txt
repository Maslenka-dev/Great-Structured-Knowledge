Для максимума:

	Текстовый файл состоит из символов, обозначающих прописные буквы латинского алфавита.
	Определите максимальное количество идущих подряд символов, среди которых никакие две буквы из набора букв N, О и Р (с учетом повторений) не записаны подряд.

		f = open('24_8510.txt')
		s = f.readline().strip().replace('O', 'N').replace('P', 'N')
		m = 0 # максимальная длина подходящей подстроки
		for i in range(len(s)): # перебираем все возможные точки старта
		    for j in range(i + m, len(s)): # перебираем все допустимые точки окончания / i + m - оптимизация
		        l = s[i : j + 1]
		        if 'NN' not in l: m = max(m, len(l)) # проверка условия и обновление m
		        elif 'NN' in l: break # проверка условия для break / оптимизация
		    if i % 100_000 == 0: print(i) # шкала прогресса
		print(m)

	Текстовый файл содержит строку из заглавных латинских букв, всего не более чем из 106 символов. Определите количество подстрок длиной не менее 17 символов, которые начинаются и заканчиваются буквой A и не содержат других букв A (кроме первой и последней) и букв B.

		f = open('24_3018.txt')
		s = f.readline().strip()
		c = 0
		for i in range(len(s)):
		    for j in range(i, len(s)):
		        l = s[i:j + 1]
		        if l[0] == 'A':
		            if 'B' not in l:
		                if l.count('A') == 2:
		                    if l[-1] == 'A':
		                        if len(l) >= 17: c += 1 # увеличение счётчика после проверки условий
		                elif l.count('A') > 2: break
		            else: break
		        else: break
		    if i % 100_000 == 0: print(i)
		print(c)

	Текстовый файл состоит из заглавных букв латинского алфавита A, B, C, D, E и F.
	Определите в прилагаемом файле максимальное количество идущих подряд символов, среди которых пара AB (в указанном порядке) встречается ровно 100 раз.
	Для выполнения этого задания следует написать программу.

		f = open('24_20909.txt')
		s = f.readline().strip()
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if l.count('AB') == 100: m = max(m, len(l))
		        elif l.count('AB') > 100: break
		    if i % 100_000 == 0: print(i)
		print(m)  

	Текстовый файл состоит из символов, обозначающих прописные буквы латинского алфавита.
	Определите в прилагаемом файле максимальное количество идущих подряд символов, среди которых гласные и согласные буквы чередуются.
	Для выполнения этого задания следует написать программу.

		gl = 'AEIOU'
		sg = 'BCDFGHJKLMNPQRSTVWXZ'
		f = open('24_11813.txt')
		s = f.readline().strip()
		for i in gl: s = s.replace(i, 'A')
		for j in sg: s = s.replace(j, 'B')
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if 'AA' not in l:
		            if 'BB' not in l: 
		                m = max(m, len(l))
		            else: break
		        else: break
		    if i % 100000 == 0: print(i)
		print(m) 

	СЛОЖНАЯ!!
	Текстовый файл состоит из десятичных цифр и заглавных букв латинского алфавита. Onределите в этом файле последовательность идущих подряд символов, представляющих собой запись максимального нечётного 12-ричного числа. В ответе запишите индекс (номер) первого символа (первой значащей цифры), с которого начинается запись этого числа в прилагаемом файле. Нумерация символов в текстовом файле начинается с нуля.
	Примечание. Латинские буквы А и В - цифры из алфавита 12-ричной системы счисления.

		f = open('24_22356.txt')
		s = f.readline().strip()
		for x in 'CDEFGHIJKLMNOPQRSTUVWXYZ':
		    s = s.replace(x, '*')
		r = []
		m = 1
		for i in range(len(s)):
		    for j in range(i + (m - 1), len(s)): 
		        l = s[i : j + 1]
		        if l[0] != '0' and '*' not in l and l[-1] in '13579B': 
		            m = max(m, len(l))
		            if len(l) == 109: 
		                r.append(l)
		                print(l)
		        elif l[0] == '0' or '*' in l: break
		print(m)
		print(s.index(max(r)))

	Текстовый файл состоит из символов A, B и C. 
	Определите максимальное количество идущих подряд пар символов AB или CB в прилагаемом файле.
	Искомая подпоследовательность должна состоять только из пар AB, или только из пар CB, или только из пар AB и CB в произвольном порядке следования этих пар.
	Для выполнения этого задания следует написать программу.

		s = open('24_7272.txt').readline().strip()
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if len(l) % 2 == 0:
		            if all(l[x] + l[x + 1] in ['AB', 'CB'] for x in range(0, len(l) - 1 , 2)):
		                m = max(m, len(l))
		            else: break
		    if i % 100_000 == 0: print(i)
		print(m / 2)	

Для минимума:

	Текстовый файл состоит не более чем из 10**6 символов и содержит только заглавные буквы латинского алфавита. Определите минимальну длину подстроки, в которой символ 2 встречается не менее 120 раз.

		f = open('24_16682.txt')
		s = f.readline().strip()
		m = 10000 # начальное значение (подбираем)
		print(len(s))
		for i in range(len(s)):
		    for j in range(i + m, i, -1):
		        l = s[i:j + 1]
		        if l.count('Z') >= 120: m = min(m, len(l))
		        if l.count('Z') < 120: break 
		print(m) 	

	В файле содержится строка длиной не более 106 из букв английского алфавита. Определите минимальную длину подстроки, в которой ровно три тройки BAD или FAT.
	Например, в строке SDFATFDBADZZSFATBADGHTBAD есть три подходящие подстроки FATFDBADZZSFAT, BADZZSFATBAD и FATBADGHTBAD. Минимальная длина равна 12.

		f = open('24_9169.txt')
		s = f.readline().strip()
		m = 10000
		for i in range(len(s)):
		    for j in range(i + m, i, -1):
		        l = s[i:j + 1]
		        if l.count('BAD') + l.count('FAT') == 3: m = min(m, len(l))
		        elif l.count('BAD') + l.count('FAT') < 3: break
		    if i % 100000 == 0: print(i)
		print(m)

	Текстовый файл состоит из символов F, G, Q, R, S и W. Определите в этом файле минимальное количество идущих подряд символов, среди которых подстрока RSQ встречается ровно 130 раз, при этом искомая последовательность не оканчивается символом Q.
	Для выполнения этого задания следует написать программу.

		f = open('24_21717.txt')
		s = f.readline().strip()
		m = 10000
		for i in range(len(s)):
		    for j in range(i + m, i, -1):
		        l = s[i:j + 1]
		        if l.count('RSQ') == 130 and l[-1] != 'Q': m = min(m, len(l))
		        elif l.count('RSQ') < 130: break
		    if i % 100000 == 0: print(i)
		print(m)

ПРАКТИКА:

	Текстовый файл содержит строку из заглавных латинских букв, всего не более чем из 106 символов. Определите количество подстрок длиной не более 15 символов, которые начинаются буквой A, содержат букву F, заканчиваются буквой B и не содержат других букв A и B, кроме первой и последней.

		s = open('24_3019.txt').readline().strip()
		print(len(s))
		c = 0
		for i in range(len(s)):
		    for j in range(i, len(s)):
		        l = s[i:j + 1]
		        if len(l) <= 15:
		            if l[0] == 'A':
		                if 'A' not in l[1:-1] and 'B' not in l[1:-1]:
		                    if 'F' in l:
		                        if l[-1] == 'B': c += 1
		                else: break
		            else: break
		        else: break 
		print(c)

	Текстовый файл состоит не более, чем из 1 200 000 прописных символов латинского алфавита. Определите максимальное количество идущих подряд символов, среди которых любые два символа из набора Q, R, S в различных комбинациях (с учётом повторений) не стоят рядом. Для выполнения этого задания следует написать программу.

		s = open('24_7600.txt').readline().strip().replace('R', 'Q').replace('S', 'Q')
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if 'QQ' not in l: m = max(m, len(l))
		        else: break
		    if i % 100000 == 0: print(i)
		print(m)

	Текстовый файл состоит из символов A, B, C, D, E и F.
	Определите максимальное количество идущих подряд символов в прилагаемом файле, среди которых пара символов CD (в указанном порядке) встречается ровно 160 раз.
	Для выполнения этого задания следует написать программу.

		s = open('24_17535.txt').readline().strip()
		print(len(s))
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if l.count('CD') == 160: m = max(m, len(l))
		        elif l.count('CD') > 160: break
		    if i % 100_000 == 0: print(i)
		print(m)

	Текстовый файл состоит из десятичных цифр и заглавных букв латинского алфавита. Определите в этом файле последовательность идущих подряд символов, представляющих собой запись максимального чётного 14-ричного числа. В ответе запишите индекс (номер) первого символа (первой значащей цифры), с которого начинается запись этого числа в прилагаемом файле. Нумерация символов в текстовом файле начинается с нуля.
	Примечание. Латинские буквы А, В, C, D - цифры из алфавита 14-ричной системы счисления.

		from string import printable
		s = open('24_22357.txt').readline().strip()
		for x in 'EFGHIJKLMNOPQRSTUVWXYZ': s = s.replace(x, '*')
		m = 1
		r = []
		for i in range(len(s)):
		    for j in range(i + (m - 1), len(s)):
		        l = s[i:j + 1]
		        if l[0] != '0':
		            if '*' not in l:
		                if int(l[-1], 14) % 2 == 0: 
		                    m = max(m, len(l))
		                    if len(l) == 112: r.append(l)
		            else: break
		        else: break
		print(s.index(max(r)))

	Текстовый файл состоит не более чем из 106 символов и содержит только заглавные буквы латинского алфавита. Определите в прилагаемом файле минимальное количество идущих подряд символов (длину непрерывной подпоследовательности), среди которых символ A встречается не менее 305 раз.
	Для выполнения этого задания следует написать программу.

		s = open('24_16787.txt').readline().strip()
		m = 10000
		for i in range(len(s)):
		    for j in range(i + m, i, -1):
		        l = s[i:j + 1]
		        if l.count('A') >= 305: m = min(m, len(l))
		        else: break
		print(m)

	Текстовый файл содержит строку из заглавных латинских букв и точек, всего не более 106 символов. Определите минимальное количество идущих подряд символов, среди которых ровно семь точек.

		s = open('24_6734.txt').readline().strip()
		m = 10000
		for i in range(len(s)):
		    for j in range(i + m, i, -1):
		        l = s[i:j + 1]
		        if l.count('.') == 7: m = min(m, len(l))
		        elif l.count('.') < 7: break
		print(m)

	Текстовый файл состоит из символов, обозначающих буквы латинского алфавита T, U, V, W, X, Y и Z.
	Определите в прилагаемом файле минимальное количество идущих подряд символов (длину непрерывной подпоследовательности), среди которых комбинация символов TYZUX встречается ровно 78 раз, при этом искомая подпоследовательность не начинается на T и не заканчивается на X.
	Для выполнения этого задания следует написать программу.

		s = open('24_23060.txt').readline().strip()
		m = 10000
		for i in range(len(s)):
		    for j in range(i + m, i, -1):
		        l = s[i:j + 1]
		        if l[0] != 'T':
		            if l.count('TYZUX') == 78:
		                if l[-1] != 'X': m = min(m, len(l))
		            elif l.count('TYZUX') < 78: break
		        else: break
		    if i % 100_000 == 0: print(i)
		print(m)

	Текстовый файл состоит из десятичных цифр и заглавных букв латинского алфавита.
	Определите в этом файле длину максимальной последовательности идущих подряд символов, где буква D встречается только один раз и в самом начале, и содержит ровно 50 цифр.

		s = open('24_23169.txt').readline().strip()
		for x in '0123456789': s = s.replace(x, '*')
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if l[0] == 'D' and l.count('D') == 1:
		                if l.count('*') == 50: m = max(m, len(l))
		                elif l.count('*') > 50: break
		        else: break
		    if i % 100_000 == 0: print(i)
		print(m)

	Текстовый файл состоит не более чем из 106 символов X, Y и Z. Определите максимальное количество идущих подряд символов, среди которых нет символов Z, а остальные символы чередуются. Для выполнения этого задания следует написать программу. 

		s = open('24_5237.txt').readline().strip()
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if 'Z' not in l:
		            if all(l[x] != l[x + 1] for x in range(len(l) - 1)): m = max(m, len(l))
		        else: break
		    if i % 100_000 == 0: print(i)
		print(m)

		ИЛИ

		s = open('24_5237.txt').readline().strip()
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if 'Z' not in l:
		            if all(l[x] + l[x + 1] in ['XY', 'YX'] for x in range(len(l) - 1)): m = max(m, len(l))
		        else: break
		    if i % 100_000 == 0: print(i)
		print(m)

		ИЛИ

		s = open('24_5237.txt').readline().strip()
		m = 0
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if 'Z' not in l:
		            if 'XX' not in l and 'YY' not in l: m = max(m, len(l))
		            else: break
		        else: break
		    if i % 100_000 == 0: print(i)
		print(m)

	В файле содержится строка длиной не более 106 из букв английского алфавита. Определите максимальную длину подпоследовательности, которая состоит только из пар символов EA, только из троек символов NPC, или из пар символов EA и троек символов NPC. Например, в строке FASEAEANPCVESEAEAEADDNPC есть три подходящие подпоследовательности EAEANPC, EAEAEA и NPC. Максимальная длина – 7.

		from string import printable
		s = open('24_8959.txt').readline().strip()
		for i in 'BDFGHIJKLMOQRSTUVWXYZ': s = s.replace(i, '*')
		m = 0
		r = []
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if '*' in l: break
		        if l.count('EA') * 2 == len(l): m = max(m, len(l))
		        if l.count('NPC') * 3 == len(l): m = max(m, len(l))
		        if (l.count('EA') * 2) + (l.count('NPC') * 3) == len(l): m = max(m, len(l))
		    if i % 100000 == 0: print(i)
		print(m)

		ИЛИ

		s = open('24_8959.txt').readline().strip()
		for i in 'BDFGHIJKLMOQRSTUVWXYZ': s = s.replace(i, '*')
		m = 0
		r = []
		for i in range(len(s)):
		    for j in range(i + m, len(s)):
		        l = s[i:j + 1]
		        if '*' in l: break
		        if l.count('EA') * 2 + l.count('NPC') * 3 == len(l): m = max(m, len(l))
		    if i % 100000 == 0: print(i)
		print(m)