    Простое число - натуральное число, большее единицы, которое не имеет никаких делителей, кроме единицы и самого себя (самого числа).  

    Нетривиальный делитель - делитель числа, который не равен 1 и самому числу.

    Функция нахождения делителей:

        def f(n):
            ds = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0: 
                    ds.add(i)
                    ds.add(n // i)
            return ds 

    С сортировкой:

        def f(n):
            ds = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0: 
                    ds.add(i)
                    ds.add(n // i)
            return sorted(ds)

    "не считая единицы и самого числа":

        def f(n):
            ds = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds

    Является ли число палиндромом:

        d = 44144
        if str(d) == str(d)[::-1]: print(True)
        else: print(False)

    Если в задаче вопрос: числа, большие 12 365 266 (например), то перебираем от 12 365 267.

    Функция нахождения множителей:

        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0: 
                    m += [d]
                    n //= d
                else: d += 1
            if n > 1: 
                m += [n]
            return m

ДЕЛИТЕЛИ

    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [154026; 154043], числа, имеющие ровно 4 различных делителя. В ответе для каждого найденного числа запишите два его наибольших делителя в порядке возрастания.

        def fd(n):
            ds = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        cnt = 0
        for i in range(6_638_225, 6_638_322 + 1):
            d = sorted(fd(i))
            if len(d) == 2:
                cnt += 1
                print(cnt, i) 

    Для интервала [33333;55555] найдите числа, которые кратны сумме своих простых делителей (не считая единицы и самого числа). В качестве ответа приведите числа, для которых сумма простых делителей больше 250, сначала найденное число, затем сумма его простых делителей.

        def fd(n):
            ds = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        for i in range(33_333, 55_555 + 1):
            d = fd(i)
            k = list(x for x in d if len(fd(x)) == 0)
            s = sum(k) 
            if s > 250:
                if i % s == 0: print(i, s)

            коротко: в 3 строке перебор идет от 2, так как "не считая единицы и самого числа". в 10 строке проверяем являются ли делители простыми числами (len(fd(x)) == 0, потому что 1 и само число не входят в множество).

        ИЛИ

        def fd(n):
            ds = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        for i in range(33_333, 55_555 + 1):
            d = list(sorted(fd(i)))
            k = list(x for x in d[1:-1] if len(fd(x)) == 2)
            s = sum(k) 
            if s > 250:
                if i % s == 0: print(i, s)

        ИЛИ

        def fd(n):
            ds = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        for i in range(33_333, 55_555 + 1):
            s = sum(list(x for x in list(sorted(fd(i)))[1:-1] if len(fd(x)) == 2))
            if s > 250:
                if i % s == 0: print(i, s)
                
    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [18782; 18822], числа, имеющие ровно три различных нечётных натуральных делителя, не считая единицы и самого числа (при этом количество чётных делителей может быть любым). Для каждого найденного числа в порядке возрастания запишите эти три делителя в таблицу на экране с новой строки. Делители в строке таблицы также должны следовать в порядке возрастания.

        def fd(n):
            ds = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        for i in range(18_782, 18_822 + 1):
            d = list(sorted(fd(i)))[1:-1]
            k = list(x for x in d if x % 2 != 0)
            if len(k) == 3:
                print(*k)

    Напишите программу, которая ищет среди целых чисел, превышающих 250200, первые пять чисел, удовлетворяющих условию: сумма максимального и минимального различных делителей, отличных от единицы и самого числа, при делении на 123 даёт остаток 17.
    В ответе запишите пять пар чисел в порядке возрастания первых чисел в паре: число и сумму максимального и минимального делителей этого числа.

        def fd(n):
            ds = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        for i in range(250_201, 253_200):
            d = list(sorted(fd(i)))[1:-1]
            if len(d) > 0 and (d[-1] + d[0]) % 123 == 17:
                print(i, d[-1] + d[0])

    Пусть М сумма минимального и максимального натуральных делителей целого числа, не считая единицы и самого числа. Если таких делителей у числа нет, то значение М считается равным нулю.
    Напишите программу, которая перебирает целые числа, большие 800 000, в порядке возрастания и ищет среди них такие, для которых значение М оканчивается на 4. В ответе запишите в первом столбце таблицы первые пять найденных чисел в порядке возрастания, а во втором столбце соответствующие им значения М.
    Например, для числа 20 M=2+10=12.

        def fd(n):
            ds = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        for i in range(800_001, 802_000):
            d = fd(i)
            if len(d) > 0:
                m = max(d) + min(d)
                if m % 10 == 4:
                    print(i, m)

    Напишите программу, которая перебирает целые числа, большие 700 000, в порядке возрастания и ищет среди них такие, у которых есть натуральный делитель, оканчивающийся на цифру 7 и не равный ни самому числу, ни числу 7. Выведите первые пять найденных чисел и для каждого соответствующий наименьший делитель, оканчивающийся на цифру 7, не равный ни самому числу, ни числу 7.
    Формат вывода: для каждого из пяти найденных чисел в отдельной строке сначала выводится само число, затем - значение наименьшего делителя, оканчивающегося на цифру 7, не равного ни самому числу, ни числу 7.
    Строки выводятся в порядке возрастания найденных чисел.

        def fd(n):
            ds = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        for i in range(700_001, 701_000):
            d = fd(i)
            if len(d) != 0:
                m = list(x for x in d if x % 10 == 7 and x != 7 and x != i)
                if len(m) > 0:
                    print(i, min(m))

    Обозначим через М разность максимального и минимального числа среди простых делителей целого числа, не считая самого числа. Если таких делителей у числа нет, то считаем значение М равным нулю. Напишите программу, которая перебирает целые числа, большие 450000, в порядке возрастания и ищет среди них такие, для которых значение М при делении на 29 даёт в остатке 11.
    Выведите первые 4 найденных числа в порядке возрастания, справа от каждого числа запишите соответствующее значения М.

        def fd(n):
            ds = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    ds.add(i)
                    ds.add(n // i)
            return ds
        for i in range(450_001, 451_000):
            d = list(x for x in fd(i) if len(fd(x)) == 0)
            if len(d) > 0:
                m = max(d) - min(d)
                if m % 29 == 11:
                    print(i, m)

    Пусть R — сумма различных натуральных делителей целого числа, не считая единицы и самого числа. Если таких делителей у числа нет, значение R принимается равным нулю.
    Напишите программу, которая перебирает целые числа больше 1 500 000 в порядке возрастания и ищет среди них такие, для которых R — простое число.
    В ответе запишите в первом столбце таблицы первые пять найденных чисел в порядке возрастания, а во втором — соответствующие им значения R.
    Например, для числа 20 R = 2 + 4 + 5 + 10 = 21.
    Количество строк в таблице для ответа избыточно.

        def fd(n):
            d = set()
            for i in range(2, int(n**0.5) + 1):
                if n % i == 0:
                    d.add(i);d.add(n // i)
            return d
        for i in range(1_500_001, 1_501_000):
            d = fd(i)
            if len(d) > 0:
                r = sum(d)
                if len(fd(r)) == 0:
                    print(i, r)

    Рассматриваются числа, бо́льшие 700 000, с ровно четырьмя различными делителями. Найдите такие числа, у которых наименьший и наибольший делители (кроме 1 и самого числа) отличаются не более чем на 15.
    В ответе запишите в первом столбце таблицы первые шесть найденных чисел в порядке возрастания, а во втором — разницу между наименьшим и наибольшим делителями (кроме 1 и самого числа).
    Количество строк в таблице для ответа избыточно.

        def fd(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i)
                    d.add(n // i)
            return d
        for i in range(700_001, 900_000):
            d = fd(i)
            if len(d) == 2:
                r = max(d) - min(d)
                if r <= 15:
                    print(i, r)

    Пусть N — сумма всех нечётных делителей числа, не считая единицы и самого числа. Если таких делителей у числа нет, то значение N считается равным нулю.
    Напишите программу, которая среди натуральных чисел, превышающих 106106, ищет первые пять чисел, для которых N больше самого числа.
    Для каждого найденного числа в отдельной строке таблицы запишите само число, а также значение N.

        def fd(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i)
                    d.add(n // i)
            return d
        for i in range(10**6, 10**6 + 2000):
            d = fd(i)
            l = list(x for x in d if x % 2 != 0)
            if len(l) > 0:
                n = sum(l)
                if n > i: print(i, n)

    Рассматриваются целые числа, принадлежащих числовому отрезку [485617; 529678], которые представляют собой произведение трёх различных простых делителей, оканчивающихся на одну и ту же цифру. В качестве ответа приведите все числа, разность максимального и минимального простых делителей которого меньше 100.
    Для каждого такого числа сначала запишите само число, а затем разность максимального и минимального простых делителей.

        from itertools import *
        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i)
                    d.add(n // i)
            return d
        for i in range(485617, 529678 + 1):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if len(f(x)) == 0)
                if len(l) > 0:
                    for x in combinations(l, 3):
                        if x[0] * x[1] * x[2] == i:
                            if x[0] % 10 == x[1] % 10 == x[2] % 10:
                                r = max(l) - min(l)
                                if r < 100:
                                    print(i, r)

    Пусть R — сумма всех различных натуральных делителей целого числа.
    Напишите программу, которая перебирает целые числа, бо́льшие 500 000, в порядке возрастания и ищет среди них такие, для которых значение RR оканчивается на цифру 6. В ответе запишите в первом столбце таблицы первые пять найденных чисел в порядке возрастания, а во втором столбце — пять соответствующих этим числам значений R.
    Например, для числа 20 R=1+2+4+5+10+20=42.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i)
                    d.add(n // i)
            return d
        for i in range(500_001, 501_000):
            d = f(i)
            r = sum(d)
            if r % 10 == 6:
                print(i, r)

    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [224466; 664422], которые делятся на 5, 7 и 13 без остатка, при этом не кратны квадрату любого из перечисленных делителей и максимальный делитель не превышает 100 000. Делители 1 и само число не учитываются. Учитываются только те числа, максимальный делитель которых оканчивается на 19.
    В качестве результата работы программы приведите найденное число и максимальный делитель этого числа.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i)
                    d.add(n // i)
            return d
        for i in range(224466, 664422 + 1):
            d = f(i)
            if len(d) > 0:
                if max(d) % 100 == 19:
                    if max(d) <= 100_000:
                        if (i % 5 == 0) and (i % 7 == 0) and (i % 13 == 0):
                            if (i % (5**2) != 0) and (i % (7**2) != 0) and (i % (13**2) != 0):
                                print(i, max(d))

    Рассматриваются целые числа из числового отрезка [326782, 965324], которые представляют собой произведение трёх различных простых делителей. Найдите такие числа, у которых разность между наименьшим и наибольшим простыми делителями меньше или равна 12.

    В ответе запишите в первом столбце таблицы найденные числа в порядке возрастания, а во втором — соответствующую разницу.

        from itertools import *
        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i)
                    d.add(n // i)
            return d
        for i in range(326782, 965324 + 1):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if len(f(x)) == 0)
                if len(l) > 0:
                    for x in combinations(l, 3):
                        if x[0] * x[1] * x[2] == i:
                            z = max(x) - min(x)
                            if z <= 12: print(i, z)

    Пусть M — сумма минимального и максимального натуральных делителей целого числа, не считая единицы и самого числа. Если таких делителей у числа нет, будем считать значение M равным нулю.
    Напишите программу, которая перебирает целые числа, большие 424 242, в порядке возрастания, ищет среди них такие, для которых значение M при делении на 2024 даёт в остатке 42, и выводит первые 8 найденных чисел и соответствующие им значения M.
    Формат вывода: для каждого из 8 таких найденных чисел в отдельной строке сначала выводится само число, затем — значение М.
    Строки выводятся в порядке возрастания найденных чисел.
    Например, для числа 20 М = 2 + 10 = 12.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i);d.add(n//i)
            return d
        for i in range(424_242, 440_000 + 1):
            d = f(i)
            if len(d) > 0:
                m = max(d) + min(d)
                if m % 2024 == 42: print(i, m)

    УБЫВАНИЕ!!
    Пусть K — сумма минимального и максимального натуральных делителей целого числа, не считая единицы и самого числа. Если таких делителей у числа нет, то считаем значение K равным нулю.
    Напишите программу, которая среди целых чисел больше 650 000 ищет числа, подходящие под условия:
        число имеет ровно 6 различных делителей, не считая единицы и самого числа
        значение K для числа имеет 4 цифры в записи
    Выведите первые 5 найденных чисел и соответствующие им значения K.
    Формат вывода: для каждого из 5 таких найденных чисел в отдельной строке сначала выводится само число, затем — значение K.
    Строки выводятся в порядке убывания найденных чисел.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(650_001, 660_000):
            d = f(i)
            if len(d) > 0:
                k = max(d) + min(d)
                if len(d) == 6:
                    if 1000 <= k <= 9999:
                        cnt += 1
                        print(cnt, i, k)

    Пусть M — сумма минимального и максимального натуральных делителей целого числа, не считая единицы и самого числа. Если таких делителей у числа нет, то считаем значение M равным нулю.
    Напишите программу, которая перебирает целые числа, бо́льшие 800 000, в порядке возрастания и ищет среди них такие, для которых M оканчивается на 4. В ответе запишите в первом столбце таблицы первые пять найденных чисел в порядке возрастания, а во втором столбце — соответствующие им значения M.
    Например, для числа 20 M=2+10=12.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(800_001, 805_000):
            d = f(i)
            if len(d) > 0:
                m = max(d) + min(d)
                if m % 10 == 4:
                    cnt += 1
                    print(cnt, i, m)

    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [25317; 51237], числа, которые имеют хотя бы 6 различных простых делителей. Делители 1 и само число не учитываются.
    Для каждого найденного числа запишите найденное число и максимальный простой делитель этого числа.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i);d.add(n // i)
            return d
        cnt = 0
        for i in range(25317, 51237):
            d = f(i)
            l = list(x for x in d if len(f(x)) == 0)
            if len(l) >= 6: 
                cnt += 1
                print(cnt, i, max(l))

    R — сумма различных простых натуральных делителей целого числа, не считая единицы и самого числа. Если таких делителей у числа нет, значение RR принимается равным нулю.
    Напишите программу, которая перебирает целые числа, бо́льшие 500 000, в порядке возрастания и ищет среди них такие, для которых RR больше 2000 и оканчивается на цифру 7.
    В ответе запишите в первом столбце таблицы первые пять найденных чисел в порядке возрастания, а во втором столбце — соответствующие им значения R.
    Например, для числа 20 R=2+5=7R=2+5=7.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i);d.add(n // i)
            return d
        cnt = 0
        for i in range(500_001, 550_000):
            d = f(i)
            l = list(x for x in d if len(f(x)) == 0)
            if len(l) > 0:
                r = sum(l)
                if r > 2000 and r % 10 == 7:
                    cnt += 1
                    print(cnt, i, r)
                    if cnt == 10: break

    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [177000; 177300], простые числа, сумма цифр которых является простым числом. Для каждого найденного числа запишите само число и сумму его цифр.

        def f(n):
            d = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0:
                    d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(177000, 177300):
            d = f(i)
            if len(d) == 2:
                j = sum(list(map(int, str(i))))
                if len(f(j)) == 2: 
                    cnt += 1
                    print(cnt, i, j)

    Пусть M — сумма минимального и максимального натуральных делителей (обязательно различных) целого числа, не считая единицы и самого числа. Если таких делителей у числа нет, то значение M признаётся равным нулю.
    Напишите программу, которая перебирает целые числа, бо́льшие 1 500 000, в порядке возрастания и ищет среди них такие, для которых MM оканчивается на 7 и не содержит цифры 2. В ответе запишите в первом столбце таблицы первые пять найденных чисел в порядке возрастания, а во втором столбце — соответствующие им значения MM.
    Например, для числа 20 M=2∗10=20.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n // i)
            return d
        cnt = 0
        for i in range(1_500_001, 1_505_001):
            d = f(i)
            if len(d) > 0:
                m = max(d) + min(d)
                if m % 10 == 7:
                    if '2' not in str(m):
                        cnt += 1
                        print(cnt, i, m)

    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [326496; 649632], числа, у которых количество четных делителей равно количеству нечетных делителей. При этом в каждой из таких групп делителей не менее 70 элементов. Для каждого найденного числа запишите само число и минимальный делитель, больший 1000.

        def f(n):
            d = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(326496, 649632):
            d = list(f(i))
            c = list(x for x in d if x % 2 == 0)
            nc = list(x for x in d if x % 2 != 0)
            if len(c) == len(nc) and len(c) >= 70 and len(nc) >= 70:
                cnt += 1
                m = list(x for x in d if x > 1000)
                print(cnt, i, min(m))

    Пусть М — разность максимального и минимального простых натуральных делителей целого числа, не считая самого числа. Если таких делителей у числа нет, то значение М считается равным нулю.
    Напишите программу, которая перебирает целые числа, большие 3 300 000, в порядке возрастания и ищет среди них такие, для которых М заканчивается на 5 и является палиндромом, то есть одинаково читается слева направо и справа налево.
    В ответе запишите в первом столбце таблицы первые пять найденных чисел в порядке возрастания, а во втором столбце — соответствующие им значения М.
    Например, для числа 298 M = 149 — 2 = 147.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1): 
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(3_300_001, 3_330_001):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if len(f(x)) == 0)
                m = max(l) - min(l)
                if m % 10 == 5:
                    if str(m) == str(m)[::-1]: 
                        cnt += 1
                        print(cnt, i, m)

    СЛОЖНАЯ!!
    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [800000, 802000], числа, у которых не менее двух простых делителей и произведение простых делителей нацело делится на сумму простых делителей. Для каждого найденного числа запишите само число и частное от деления произведения его простых делителей на сумму простых делителей.

        from math import *
        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n // i)
            return d
        cnt = 0
        for i in range(800000, 802000):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if len(f(x)) == 0)
                if len(l) >= 2:
                    m = prod(l) / sum(l)
                    if prod(l) % sum(l) == 0:
                        cnt += 1
                        print(cnt, i, int(m))

    Пусть R — сумма всех делителей числа, оканчивающихся на 8, не считая самого числа. Если таких делителей у числа нет, то значение R считается равным нулю.
    Напишите программу, которая среди натуральных чисел на отрезке [114578, 114616] ищет все числа, для которых значение R оканчивается на 6.
    Для каждого найденного числа в отдельной строке таблицы запишите само число, а также значение R.

        def f(n):
            d = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n // i)
            return d
        cnt = 0
        for i in range(114578, 114616 + 1):
            d = list(f(i))
            l = list(x for x in d if x % 10 == 8 if x != i)
            if len(l) > 0: 
                r = sum(l)
                if r % 10 == 6: 
                    cnt += 1
                    print(cnt, i, r)

    Для интервала [33333; 55555] найдите числа, которые кратны сумме своих простых делителей (не считая единицы и самого числа).
    В качестве ответа приведите числа, для которых сумма простых делителей больше 250, — сначала найденное число, затем сумма его простых делителей.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(33333, 55555 + 1):
            d = f(i)
            if len(d) > 0:
                l = sum(list(x for x in d if len(f(x)) == 0))
                if i % l == 0:
                    if l > 250:
                        cnt += 1
                        print(cnt, i, l)

    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [174457; 174505], числа с двумя различными натуральными делителями, не считая единицы и самого числа.
    Для каждого найденного числа запишите эти два делителя в таблицу на экране с новой строки в порядке возрастания произведения этих двух делителей.
    Делители в строке таблицы также должны следовать в порядке возрастания.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(174457, 174505 + 1):
            d = sorted(f(i))
            if len(d) == 2:
                cnt += 1
                print(cnt, d)

    Напишите программу, которая перебирает целые числа, бо́льшие 800 000, в порядке возрастания и ищет среди них такие, у которых есть натуральный делитель, оканчивающийся на 14 и не равный ни самому числу, ни числу 14.
    Требуется вывести первые пять найденных чисел и для каждого — наименьший их делитель, оканчивающийся на 14 и не равный ни самому числу, ни числу 14.
    Формат вывода: для каждого из пяти таких найденных чисел в отдельной строке сначала выводится само число, затем — значение наименьшего делителя, оканчивающегося на 14 и не равного ни самому числу, ни числу 14.
    Строки выводятся в порядке возрастания найденных чисел.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(800_000, 802_000):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if x % 100 == 14 and x != i and x != 14)
                if len(l) > 0:
                    cnt += 1
                    print(cnt, i, min(l))

    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [333555; 777999], числа, среди делителей которых есть ровно 35 двузначных чисел. Для каждого найденного числа запишите наименьший и наибольший из них. Так, например, для числа 36 учитываются только делители 12 и 18.
    Следовательно, для него необходимо вывести два числа:
    12 18

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(333555, 777999):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if 10 <= x <= 99)
                if len(l) == 35:
                    cnt += 1
                    print(cnt, min(l), max(l))

    Пусть M – сумма минимального и максимального натуральных делителей целого числа, не считая единицы и самого числа. Если таких делителей у числа нет, то считаем значение M равным нулю. 
    Напишите программу, которая перебирает целые числа, большие 452 021, в порядке возрастания и ищет среди них такие, для которых значение M при делении на 7 даёт в остатке 3. Вывести первые 5 найденных чисел и соответствующие им значения M.  
    Формат вывода: для каждого из 5 таких найденных чисел в отдельной строке сначала выводится само число, затем – значение М. 
    Строки выводятся в порядке возрастания найденных чисел. 
    Например, для числа 20 М = 2 + 10 = 12.  

        def f(n):
            d = set()
            for i in range(2, int(n**0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(452_022, 460_000):
            d = f(i)
            if len(d) > 0:
                m = min(d) + max(d)
                if m % 7 == 3:
                    cnt += 1
                    print(i, m)
                    if cnt > 10: break

    СЛОЖНАЯ!!
    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [190201; 190260], числа, имеющие ровно 4 различных чётных делителя. В ответе для каждого найденного числа в порядке возрастания запишите два его наибольших чётных делителя. Делители в строке должны следовать в порядке убывания.

        def f(n):
            d = set()
            for i in range(1, int(n**0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        for i in range(190201, 190260 + 1):
            d = f(i)
            if len(d) > 0:
                l = sorted(list(x for x in d if x % 2 == 0))
                if len(l) == 4:
                    m = l[-2:]
                    print(i, *m[::-1])

    Обозначим через F целую часть среднего арифметического всех натуральных делителей целого числа, кроме единицы и самого числа. Если таких делителей у числа нет, то считаем значение F равным нулю. Напишите программу, которая перебирает целые числа, большие 550000, в порядке возрастания и ищет среди них такие, для которых значение F при делении на 31 даёт в остатке 13.
    Выведите первые 5 найденных чисел в порядке возрастания и справа от каждого числа – соответствующее значение F.

        def f(n):
            d = set()
            for i in range(2, int(n**0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        for i in range(550_001, 550_400):
            d = f(i)
            if len(d) > 0:
                F = int(sum(d) / len(d))
                if F % 31 == 13: 
                    print(i, F)

    Пусть S - сумма различных натуральных делителей целого числа, являющихся простыми числами (не считая самого числа). 
    Напишите программу, которая перебирает числа, большие 670 000, в порядке возрастания и ищет среди них такие, для которых значение S оканчивается на цифру 5. Программа должна найти и вывести первые 5 таких чисел и соответствующие им значения S. 
    Формат вывода: для каждого числа выводится сначала само число, затем соответствующее ему значение S в порядке возрастания найденных чисел. 

        def f(n):
            d = set()
            for i in range(2, int(n**0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(670_001, 700_000):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if len(f(x)) == 0)
                if len(l) > 0:
                    s = sum(l)
                    if s % 10 == 5:
                        cnt += 1
                        print(i, s)
                        if cnt > 10: break

    Напишите программу, которая перебирает целые числа, большие 800000, в порядке возрастания и ищет среди них такие, у которых есть натуральный делитель, оканчивающийся на цифру 9 и не равный ни самому числу, ни числу 9. В ответе запишите в первом столбце таблицы первые пять найденных чисел в порядке возрастания, а во втором столбце — соответствующий минимальный делитель для каждого числа, оканчивающийся цифрой 9, не равный ни самому числу, ни числу 9.
    Строки выводятся в порядке возрастания найденных чисел.

        def f(n):
            d = set()
            for i in range(1, int(n**0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(800001, 801_000):
            d = f(i)
            l = list(x for x in d if x % 10 == 9 and x != 9 and x != i)
            if len(l) >= 1:
                cnt += 1
                print(i, min(l))
                if cnt > 10: break

    СЛОЖНАЯ!!
    Напишите программу, которая перебирает целые числа, большие 15 381 264, в порядке возрастания и ищет среди них числа, представленные в виде произведения ровно трёх простых множителей, не обязательно различных, каждый из которых содержит в своей записи ровно одну цифру 1.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из чисел соответствующий им наибольший из найденных множителей.

        from itertools import product
        from math import prod
        def f(n):
            d = set()
            for i in range(2, int(n**0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(15_381_264, 15_385_264):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if len(f(x)) == 0 if str(x).count('1') == 1)
                if len(l) > 0:
                    for x in product(l, repeat = 3):
                        if prod(x) == i: 
                            print(i, max(l))
                            break

    Пусть S – сумма всех простых натуральных делителей целого числа, не считая самого числа. Если таких делителей у числа нет, то считаем значение S равным нулю.
    Напишите программу, которая перебирает целые числа, меньшие 1 475 000, в порядке убывания и ищет среди них такие, для которых значение S не равно нулю, не больше 42 000 и кратно 6 В ответе запишите первые пять найденных чисел в порядке убывания, справа от них соответвующие значения S.
    Например, для числа 10 S = 2 + 5 = 7

        def f(n):
            d = set()
            for i in range(2, int(n**0.5) + 1):
                if n % i == 0: d.add(n//i);d.add(i)
            return d
        cnt = 0
        for i in range(1_475_000 - 1, 1_470_000, -1):
            d = f(i)
            if len(d) > 0:
                l = list(x for x in d if len(f(x)) == 0)
                if len(l) > 0:
                    S = sum(l)
                    if S != 0 and S <= 42_000 and S % 6 == 0: 
                        cnt += 1
                        print(cnt, i, S)

    Обозначим через M сумму двух минимальных и двух максимальных простых делителей целого числа, не считая самого числа. Если количество простых делителей у числа меньше 4, то считаем значение M равным нулю. Напишите программу, которая перебирает целые числа, большие 456 789, в порядке возрастания и ищет среди них такие, для которых значение M при делении на 114 даёт в остатке 39.
    Выведите первые 5 найденных чисел в порядке возрастания, справа от каждого числа запишите соответствующее значения M.  
    Количество строк для записи ответа избыточно.

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(456_790, 10**10):
            d = list(f(i))
            l = list(x for x in d if len(f(x)) == 0)
            if len(l) >= 4:
                m = l[0] + l[1] + l[-1] + l[-2]
                if m % 114 == 39:
                    cnt += 1
                    print(i, m)
                    if cnt == 5: break  

    СЛОЖНАЯ!!
    Найдите числа большие 800000, сумма и произведение делителей которых нечётны. В ответе укажите 6 наименьших из найденных чисел, количество делителей которых больше 10. Для каждого найденного числа выведите количество его делителей.
    Формат выходных данных: найденные числа в порядке возрастания. Справа от каждого найденного числа число его делителей.

        from math import prod
        def f(n):
            d = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i); d.add(n // i)
            return d
        cnt = 0
        for i in range(800_001, 10**10):
            d = f(i)
            if len(d) > 10:
                if sum(d) % 2 != 0 and prod(d) % 2 != 0:
                    cnt += 1
                    print(i, len(d))
                    if cnt == 6: break

    Обозначим через A целую часть среднего арифметического всех делителей целого числа, не считая самого числа. Напишите программу, которая перебирает целые числа, меньшие 770000, в порядке убывания и ищет среди них такие, для которых значение A оканчивается на 12. Выведите первые 5 найденных чисел и соответствующие им значения A.  
    Формат вывода: для каждого из 5 таких найденных чисел в отдельной строке сначала выводится само число, затем – значение A. Строки выводятся в порядке убывания найденных чисел. 
    Например, для числа 8 значение A = (1 + 2 + 4) / 3 = 2.  

        def f(n):
            d = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(769999, 0, -1):
            d = f(i)
            l = list(x for x in d if x != i)
            if len(l) > 0:
                A = int(sum(l) / len(l))
                if A % 100 == 12: 
                    cnt += 1
                    print(i, A)
                    if cnt == 5: break

    Пусть S - сумма различных натуральных делителей целого числа, являющихся простыми числами, не считая самого числа.
    Напишите программу, которая перебирает целые числа, меньшие 500 000, в порядке убывания и ищет среди них такие, для которых значение S не равно нулю и делится на 10. Программа должна найти и вывести первые 7 таких чисел и соответствующие им значения S.
    формат вывода: для каждого из 7 таких найденных чисел в отдельной строке сначала выводится само число, затем значение S. Строки выводятся в порядке убывания найденных чисел.
    Например, для числа 20 S = 2 + 5 =7

        def f(n):
            d = set()
            for i in range(2, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        cnt = 0
        for i in range(499999, 0, -1):
            d = f(i)
            l = list(x for x in d if len(f(x)) == 0)
            S = sum(l)
            if S != 0 and S % 10 == 0: 
                cnt += 1
                print(i, S)
                if cnt == 7: break

    Напишите программу, которая ищет среди целых чисел, принадлежащих числовому отрезку [333555; 777999], числа, среди делителей которых есть ровно 35 двузначных чисел. Для каждого найденного числа запишите наименьший и наибольший из них. Так, например, для числа 36 учитываются только делители 12 и 18.

        def f(n):
            d = set()
            for i in range(1, int(n ** 0.5) + 1):
                if n % i == 0: d.add(i);d.add(n//i)
            return d
        for i in range(333555, 777999 + 1):
            d = f(i)
            l = list(x for x in d if 10 <= x <= 99)
            if len(l) == 35:
                print(min(l), max(l)    

МНОЖИТЕЛИ

    Напишите программу, которая перебирает целые числа, большие 13 475 124, в порядке возрастания и ищет среди них числа, представленные в виде произведения 5 простых множителей, не обязательно различных, каждый из которых содержит в своей записи хотя бы одну цифру 5.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из них соответствующий наибольший из найденных множителей.
    Количество строк в таблице для ответа избыточно.

        from math import prod
        def f(n):
            m = []
            d = 2
            while d ** 2 <= n:
                if n % d == 0:
                    m += [d]
                    n //= d
                else: d += 1
            if n > 1: m += [n]
            return m
        cnt = 0
        for i in range(13_475_125, 13_500_125):
            m = f(i)
            if len(m) == 5:
                l = list(x for x in m if str(x).count('5') >= 1 and len(f(x)) == 1)
                if prod(l) == i:
                    cnt += 1
                    print(cnt, i, max(l))

    Напишите программу, которая перебирает целые числа, большие 7 305 678, в порядке возрастания и ищет среди них числа, представленные в виде произведения ровно четырех простых множителей, не обязательно различных, сумма которых является числом палиндромом.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из чисел соответствующие ему сумму делителей.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0:
                    m += [d]
                    n //= d
                else: d += 1
            if n > 1: m += [n]
            return m
        cnt = 0
        for i in range(7_305_679, 10**10):
            m = f(i)
            if len(m) == 4:
                sd = sum(m)
                if str(sd) == str(sd)[::-1]:
                    cnt += 1
                    print(i, sd, m)
                    if cnt == 5: break

    Напишите программу, которая перебирает целые числа, большие 12 365 266, в порядке возрастания и ищет среди них те, которые можно представить в виде произведения ровно пяти различных простых множителей, а сумма этих множителей образует простое число.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, во втором столбце — сумму их простых множителей.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0: m += [d]; n//=d
                else: d += 1
            if n > 1: m += [n]
            return m
        cnt = 0
        for i in range(12_365_267, 10**10):
            m = f(i)
            if len(m) == 5 and all(m.count(x) == 1 for x in m):
                s = sum(m)
                if len(f(s)) == 1:
                    cnt += 1
                    print(i, s)
                    if cnt == 5: break

    Напишите программу, которая перебирает целые числа, большие 6 086 055, в порядке возрастания и ищет среди них числа, представленные в виде произведено двух простых множителей, не обязательно различных, каждый из которых содержит в своей записи ровно одну цифру 6.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из них соответствующий наибольший из найденных множителей.
    Количество строк в таблице для ответа избыточно

        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0: m += [d]; n //= d
                else: d += 1
            if n > 1: m += [n]
            return m
        cnt = 0
        for i in range(6_086_056, 10**10):
            m = f(i)
            if len(m) == 2 and all(str(x).count('6') for x in m):
                cnt += 1
                print(i, max(m))
                if cnt == 5: break

    Напишите программу, которая перебирает целые числа, большие 6 651 220, в порядке возрастания и ищет среди них числа, представленные в виде произведения ровно двух простых множителей, не обязательно различных, каждый из которых содержит в своей записи ровно одну цифру 2.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из чисел соответствующий им наибольший из найденных множителей.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0: m += [d]; n //= d
                else: d += 1
            if n > 1: m += [n]
            return m
        cnt = 0
        for i in range(6_651_221, 10**10):
            m = f(i)
            if len(m) == 2 and all(str(x).count('2') == 1 for x in m):
                cnt += 1
                print(i, max(m))
                if cnt == 5: break

    Напишите программу, которая перебирает целые числа, большие 1 324 727, в порядке возрастания и ищет среди них числа, представленные в виде произведения ровно двух простых множителей, не обязательно различных, каждый из которых содержит в своей записи ровно одну цифру 5.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из чисел наибольший из соответствующих им найденных множителей.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0: 
                    m += [d]
                    n //= d
                else: d += 1
            if n > 1: 
                m += [n]
            return m
        cnt = 0
        for i in range(1_324_728, 10**10):
            m = f(i)
            if len(m) == 2 and all(str(x).count('5') == 1 for x in m):
                cnt += 1
                print(i, max(m))
                if cnt == 5: break

    Напишите программу, которая перебирает целые числа, большие 24 517 512, в порядке возрастания и ищет среди них числа, представленные в виде произведения 12 простых множителей, не обязательно различных.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из них соответствующий наибольший из найденных множителей.
    Количество строк в таблице для ответа избыточно.

        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0: m += [d]; n //= d
                else: d += 1
            if n > 1: m += [n]
            return m
        cnt = 0
        for i in range(24_517_513, 10**10):
            m = f(i)
            if len(m) == 12:
                cnt += 1
                print(i, max(m))
                if cnt == 5: break

    Напишите программу, которая перебирает целые числа, большие 1 561 330, в порядке возрастания и ищет среди них числа, представленные в виде произведения ровно трёх простых множителей, не обязательно различных, сумма которых является палиндромом, т.е. одинаково читается слева направо и справа налево.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из чисел соответствующую им сумму всех их простых множителей.

        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0: m += [d]; n //= d
                else: d += 1
            if n > 1: m += [n]
            return m
        cnt = 0
        for i in range(1561331, 10**10):
            m = f(i)
            l = list(x for x in m if len(f(x)) == 1)
            if len(l) == 3 and str(sum(l)) == str(sum(l))[::-1]:
                cnt += 1
                print(i, sum(l))
                if cnt == 5: break

    Напишите программу, которая перебирает целые числа, большие 1 379 210, в порядке возрастания и ищет среди них числа, представленные в виде произведения ровно пяти простых множителей, не обязательно различных, каждый из которых содержит в своей записи хотя бы одну цифру 3.
    В ответе в первом столбце таблицы запишите первые 5 найденных чисел в порядке возрастания, а во втором столбце - для каждого из чисел соответствующий им наибольший из найденных множителей.

        from math import *
        def f(n):
            d = 2
            m = []
            while d**2 <= n:
                if n % d == 0: m += [d]; n //= d
                else: d += 1
            if n > 1: m += [n]
            return m
        cnt = 0
        for i in range(1379210, 10**10):
            m = f(i)
            if len(m) == 5 and all(str(x).count('3') >= 1 for x in m):
                cnt += 1
                print(i, max(m))
                if cnt == 5: break