В некоторой системе счисления с основанием выполняется равенство BOp + OMp + BL4p = CNGp p < 37
Определите значение р и укажите его в ответе в десятичной системе счисления.

	for p in range(36, 10, -1):
	    left = int('BO', p) + int('OM', p) + int('BL4', p) 
	    right = int('CNG', p)
	    if left == right: 
	        print(p)
	        break

Операнды арифметического выражения записаны в системе счисления с основанием 24.
В записи чисел переменной х обозначена неизвестная цифра из алфавита 24-ричной системы счисления. Определите наименьшее значение х, при котором значение данного арифметического выражения кратно 23. Для найденного х вычислите частное от деления значения арифметического выражения на 23 и укажите его в ответе в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	for x in printable[:24]:
	    a = int(f'4m{x}f', 24)
	    b = int(f'265afdn{x}', 24)
	    c = int(f'n4{x}931b3l', 24)
	    d = int(f'ng{x}4f', 24)
	    f = int(f'fkjb5{x}ik', 24)
	    r = a + b + c + d + f
	    if r % 23 == 0:
	        print(r / 23)

Операнды арифметического выражения записаны в системах счисления с основаниями 21. В записи чисел переменными х и у обозначены неизвестные цифры из алфавита 21-ричной системы счисления. Определите наименьшее значение х, при которых значение данного арифметического выражения кратно 1810 при любом значении у. Для найденного значения х вычислите частное от деления значения арифметического выражения на 1810 при у-5 и укажите его в ответе в десятичной системе счисления.

	from string import printable
	for x in printable[:21]:
	        if all((int(f'12{y}{x}9', 21) + int(f'36{y}99', 21)) % 18 == 0 for y in printable[:21]):
	                print(x)
	print((int(f'12539', 21) + int(f'36599', 21)) / 18)

Дано арифметическое выражение. В записи чисел переменной х обозначена неизвестная цифра из допустимого алфавита для указанных систем счисления. Определите наибольшее значение х, при котором значение данного арифметического выражения кратно 123. Для найденного значениях вычислите частное от деления значения арифметического выражения на 123 и укажите его в ответе в десятичной системе счисления.

	def f(n, b):
	    r = n[::-1]
	    a = 0     
	    for i in range(0, len(n)):
	        a += r[i] * b**i
	    return a
	for x in range(0, 98):
	    a = f([1,2,x,4,5], 98) + f([1,x,9,8], 123)
	    if a % 123 == 0:
	        print(a / 123, x)

Определите число n, для которого выполняется равенство 110n = 39800_10.

	def f(n, b):
	    r = n[::-1]
	    a = 0
	    for i in range(0, len(n)):
	        a += r[i] * b**i
	    return a
	for n in range(1, 1000):
	    if f([1,1,0], n) == 39800:
	        print(n)

СЛОЖНАЯ!!
Операнды арифметического выражения записаны в системе счисления с основанием 190.
NxW190+YyAR190
В записи чисел переменными x и y обозначены неизвестные цифры из алфавита 190-ричной системы счисления. Определите наибольшее значение произведения x на y, при котором значение этого арифметического выражения кратно 189.
Для найденных x и y вычислите частное от деления значения арифметического выражения на 189 и укажите его в ответе в десятичной системе счисления.
Основание системы счисления указывать не нужно.

	from string import printable
	def f(n, b):
	    r = n[::-1]
	    a = 0
	    for i in range(0, len(n)):
	        a += r[i] * b**i
	    return a
	l = []
	for x in range(0, 190):
	    for y in range(0, 190):
	        a = f([printable.index('n'), x, printable.index('w')], 190)
	        b = f([printable.index('y'), y, printable.index('a'), printable.index('r')], 190)
	        r = a + b
	        if r % 189 == 0: l.append([x * y, x, y, r / 189])
	print(sorted(l)[-1])

СЛОЖНАЯ!!
Результат выражения 3Bz4C35+A12Fy19 делится на 7, где z и y — цифры в используемых системах счисления.
Найдите максимальное значение суммы z+y, когда это возможно.
В качестве ответа приведите десятичную запись полученной суммы.

	from string import printable
	r = []
	for z in printable[:35]:
	    for y in printable[:19]:
	        a = int(f'3b{z}4c', 35) + int(f'a12f{y}', 19)
	        if a % 7 == 0: r.append(int(z, 35) + int(y, 19))
	print(max(r))

СЛОЖНАЯ!!
Результат выражения 52xH1232+22x832 делится нацело на 1y16. Сколько различных значений может принимать сумма x+y?

	from string import printable
	r = set()
	for x in printable[:32]:
	    for y in printable[:16]:
	        a = int(f'52{x}h12', 32) + int(f'22{x}8', 32)
	        b = int(f'1{y}', 16)
	        if a % b == 0: r.add(int(x, 32) + int(y, 16))
	print(len(r))

Операнды арифметического выражения записаны в системе счисления с основанием 23.
324x7223+45x56223
В записи чисел переменной х обозначена неизвестная цифра из алфавита системы счисления с основанием 23.
Определите наименьшее значение х, при котором значение данного арифметического выражения кратно 22. Для найденного х вычислите частное от деления значения арифметического выражения на 22 и укажите его в ответе в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	for x in printable[:23]:
	    a = int(f'324{x}72', 23) + int(f'45{x}562', 23)
	    if a % 22 == 0: print(x, a / 22)

Операнды арифметического выражения записаны в системе счисления с основанием 15.
99658x2915+102x02315
В записи чисел переменной x обозначена неизвестная цифра из алфавита пятнадцатеричной системы счисления.
Определите наибольшее значение x, при котором значение данного арифметического выражения кратно 14. Для найденного значения x вычислите частное от деления значения арифметического выражения на 14 и укажите его в ответе в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	for x in printable[:15]:
	    a = int(f'99658{x}29', 15) + int(f'102{x}023', 15)
	    if a % 14 == 0: print(x, a / 14)

СЛОЖНАЯ!!
Операнды арифметического выражения записаны в системе счисления с основанием 18.
56x318+4x918−57x118
В записи чисел переменной xx обозначена неизвестная цифра из алфавита 18-ричной системы счисления. Определите количество значений переменной x, при которых значение данного арифметического выражения является простым числом.

	from string import printable
	def f(n):
	    a = set()
	    for i in range(2, int(n**0.5) + 1):
	        if n % i == 0:
	            a.add(i)
	            a.add(n // i)
	    return a
	r = set()
	for x in printable[:18]:
	    a = int(f'56{x}3', 18) + int(f'4{x}9', 18) - int(f'57{x}1', 18)
	    if len(f(a)) == 0: r.add(x)
	print(len(r))

Операнды арифметического выражения записаны в системе счисления с основанием 24.
В записи чисел переменной x обозначена неизвестная цифра из алфавита 24-ричной системы счисления. Определите наименьшее значение x, при котором значение данного арифметического выражения кратно 23. Для найденного x вычислите частное от деления значения арифметического выражения на 23 и укажите его в ответе в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	for x in printable[:24]:
	    a = int(f'4m{x}f', 24) + int(f'265afdn{x}', 24) + int(f'n4{x}931b3l', 24)
	    b = int(f'ng{x}4f', 24) + int(f'fkjb5{x}ik', 24)
	    r = a + b
	    if r % 23 == 0: print(r / 23)

Операнды арифметического выражения записаны в системе счисления с основанием 13.
537x62313−6x35x213
В записи чисел переменной xx обозначена неизвестная цифра из алфавита 13-ричной системы счисления. Определите наибольшее значение xx, при которой значение данного арифметического выражения кратно 3. Ответ укажите в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	for x in printable[:13]:
	    a = int(f'537{x}623', 13) - int(f'6{x}35{x}2', 13)
	    if a % 3 == 0: print(x)

Операнды арифметического выражения записаны в системе счисления с основанием 25.
B7x35F125+16xK125+1xG25
В записи чисел переменной xx обозначена неизвестная цифра из алфавита 25-ричной системы счисления. Определите наибольшее значение xx, при котором значение данного арифметического выражения кратно 24. Для найденного значения xx вычислите частное от деления значения арифметического выражения на 24 и укажите его в ответе в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	for x in printable[:25]:
	    a = int(f'b7{x}35f1', 25) + int(f'16{x}k1', 25) + int(f'1{x}g', 25)
	    if a % 24 == 0: print(a / 24)

Операнды арифметического выражения записаны в системе счисления с основанием 17.
149x317+x61217−x54x17
В записи чисел переменной xx обозначена неизвестная цифра из алфавита 17-ричной системы счисления. Определите сумму всех значений x, при которых значение данного арифметического выражения кратно 7. В ответе укажите значение суммы в десятичной системе счисления. Основание системы счисления в ответе указывать не нужно.

	from string import printable
	r = []
	for x in printable[:17]:
	    a = int(f'149{x}3', 17) + int(f'{x}612', 17) - int(f'{x}54{x}', 17)
	    if a % 7 == 0: r.append(int(x, 17))
	print(sum(r))
	print(sum(set(r)))

Операнды арифметического выражения записаны в системе счисления с основанием 29.
463x792129+8241x15329
В записи чисел переменной хх обозначена неизвестная цифра из алфавита 29-ричной системы счисления. Определите наименьшее значение хх, при котором значение данного арифметического выражения кратно 28.
Для найденного хх вычислите частное от деления значения арифметического выражения на 28 и укажите его в ответе в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	for x in printable[:29]:
	    a = int(f'463{x}7921', 29) + int(f'8241{x}153', 29)
	    if a % 28 == 0: print(x, a / 28)

Операнды арифметического выражения записаны в системе счисления с основанием 15.
97968x1315+7x21315
В записи чисел переменной x обозначена неизвестная цифра из алфавита 15-ричной системы счисления. Определите все значения x, при которых значение данного арифметического выражения кратно 11. В ответе укажите сумму найденных чисел в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	r = []
	for x in printable[:15]:
	    a = int(f'97968{x}13', 15) + int(f'7{x}213', 15)
	    if a % 11 == 0: r.append(int(x, 15))
	print(r)
	print(sum(r))

Операнды арифметического выражения записаны в системе счисления с основанием 16.
27Ax2316+8yE5D216
В записи чисел переменными x,yx,y обозначены неизвестные цифры из алфавита 16-ричной системы счисления. Определите наибольшее значение суммы xx и yy, при которой значение данного арифметического выражения кратно 5. Ответ укажите в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	r = []
	for x in printable[:16]:
	    for y in printable[:16]:
	        a = int(f'27a{x}23', 16) + int(f'8{y}e5d2', 16)
	        if a % 5 == 0: r.append(int(x, 16) + int(y, 16))
	print(max(r))

Операнды арифметического выражения записаны в системе счисления с основанием 20.
627xJ820+H45Ix5HIJ20+4IDB49Jx720
В записи чисел переменной x обозначена неизвестная цифра из алфавита 20-ричной системы счисления. Определите наибольшее значение x, при котором значение данного арифметического выражения кратно 19. Для найденного x вычислите частное от деления значения арифметического выражения на 19 и укажите его в ответе в десятичной системе счисления. Основание системы счисления указывать не нужно.

	from string import printable
	r = []
	for x in printable[:20]:
	    a = int(f'627{x}j8', 20) + int(f'h45i{x}5hij', 20) + int(f'4idb49j{x}7', 20)
	    if a % 19 == 0: r.append([x, a / 19])
	print(max(r))

В некоторых системах счисления, для записи оснований которых используются целочисленные переменные p < 37 и s < 35, выполняется равенство:
R4p−1+B0s+2+T3NK4p=2359339910R4p−1
Определите произведение подходящих значений p и s и укажите его в ответе в десятичной системе счисления.

	from string import printable
	for p in range(36, 3, -1):
	    for s in range(34, 9, -1):
	        a = int('r4', p - 1) + int('b0', s + 2) + int('t3nk4', p)
	        b = 23593399
	        if a == b: 
	            print(p * s)
	            break

    ИЛИ

	from string import printable
	pp = [printable.index('r'), printable.index('t'), printable.index('n'), printable.index('k')]
	ss = [printable.index('b')]
	for p in range(36, max(pp), -1):
	    for s in range(34, ss[0], -1):
	        a = int('r4', p - 1) + int('b0', s + 2) + int('t3nk4', p)
	        b = 23593399
	        if a == b: 
	            print(p * s)
	            break

СЛОЖНАЯ!!
Дано арифметическое выражение:
3x2181+17x467
В записи чисел переменной x обозначена неизвестная цифра из допустимого алфавита для указанных систем счисления. Определите наибольшее значение x, при котором значение данного арифметического выражения кратно 35. Для найденного значения x вычислите частное от деления значения арифметического выражения на 35 и укажите его в ответе в десятичной системе счисления

	def f(n, b):
	    r = n[::-1]
	    a = 0
	    for i in range(0, len(n)):
	        a += r[i] * b ** i
	    return a
	r = []
	for x in range(1, 67):
	    a = f([3, x, 2, 1], 81) + f([1, 7, x, 4], 67)
	    if a % 35 == 0: r.append([x, a / 35])
	print(max(r))

СЛОЖНАЯ!!
Операнды арифметического выражения записаны в системах счисления с основаниями 15 и 17.
123x515+67y917
В записи чисел переменными x и y обозначены неизвестные цифры из алфавитов 15-ричной и 17-ричной систем счисления соответственно. Определите значения x, y, при которых значение данного арифметического выражения кратно 131. Для найденных значений x, y вычислите частное от деления значения арифметического выражения на 131 и укажите его в ответе в десятичной системе счисления. Если можно выбрать x, y не единственным образом, возьмите ту пару, в которой значение y меньше. Основание системы счисления в ответе указывать не нужно.

	from string import printable
	r = []
	for x in printable[:15]:
	    for y in printable[:17]:
	        a = int(f'123{x}5', 15) + int(f'67{y}9', 17)
	        if a % 131 == 0: r.append([y, a / 131])
	print(sorted(r))

Значение выражения 7**500−53N кратно 6. При каком минимальном значении N это возможно?
	
	for i in range(6, 36):
    if (7**500 - int('53', i)) % 6 == 0: print(i)

    ИЛИ

	def f(n, b):
	    r = n[::-1]
	    a = 0
	    for i in range(0, len(n)):
	        a += r[i] * b**i
	    return a
	for n in range(1, 200):
	    l = 7**500 - f([5, 3], n)
	    if l % 6 == 0: 
	        if n > 6: print(n)

ОЧЕНЬ СЛОЖНАЯ!!
Дано арифметическое выражение:
73x1y67+49y6x
В записи чисел переменными x и y обозначены неизвестные цифры из допустимого алфавита для указанных систем счисления. Определите, сколько различных значений может принимать выражение при всех возможных x и y.

	def f(n, b):
	    r = n[::-1]
	    a = 0
	    for i in range(0, len(n)):
	        a += r[i] * b ** i
	    return a
	m = set()
	for x in range(10, 67):
	    for y in range(0, x):
	        m.add(f([7,3,x,1,y], 67) + f([4,9,y,6], x))
	print(len(m))

	Во втором выражении есть цифра 9, поэтому система счисления должна быть больше 9.
	Значение y во втором выражении должно быть меньше x, потому что x - показатель системы счисления.