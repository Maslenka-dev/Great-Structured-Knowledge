Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
- символ "?" означает ровно одну произвольную цифру.
- символ "*" означает любую последовательность цифр произвольной длины. Кроме того, "*" может задавать и пустую последовательность.
Например, маске 123*475 соответствуют числа 123405 и 12300405.
Среди натуральных чисел, не превышающих 109, найдите все числа, соответствующие маске 273475678, делящиеся на число 151 без остатка.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце-
соответствующие им результаты деления этих чисел на 151.

	from fnmatch import *
	for i in range(151, 10**9 + 1, 151):
	    if fnmatch(str(i), '2?34?56?8'):
	        print(i, i // 151) 

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие
символы:
символ «?» означает ровно одну произвольную цифру;
символ «#» означает последовательность из ровно трёх произвольных цифр.
Например, маске 123#475 соответствуют числа 123333405 и 123001405. Среди натуральных чисел, не превышающих 1010, найдите все числа, соответствующие маске 21#68779, делящиеся де на 1777 без остатка. В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце соответствующие им результаты деления этих чисел на 1777.

	from fnmatch import *
	for i in range(1777, 10**10 + 1, 1777):
	    if fnmatch(str(i), '21???68?79'):
	        print(i, i // 1777)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
символ «?» означает ровно одну произвольную цифру;
символ «#» означает любую последовательность нечётных цифр произвольной длины; в том числе «#» может задавать и пустую последовательность.
Например, маске 123#475 соответствуют числа 123405 и 12300405.
Среди натуральных чисел, не превышающих 1010, найдите все числа, соответствующие маске 346#2772 и делящиеся на 2004 без остатка.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце соответствующие им
результаты деления этих чисел на 2004.

	for i in range(2004, 10**10 + 1, 2004):
	    if str(i)[0:3] == '346' and str(i)[-3:] == '772' and \
	    all(int(x) % 2 != 0 for x in str(i)[3:-4]) and len(str(i)) >= 8:
	        print(i, i // 2004)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие
символы:
-символ «?» означает ровно одну произвольную цифру;
- символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*475 соответствуют числа 123405 и 12300405.
Среди натуральных чисел, не превышающих 1010, найдите все числа с нечётной суммой цифр, соответствующие
маске 112257*4 и делящиеся на 2024 без остатка.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце
- соответствующие им результаты деления этих чисел на 2024.

	from fnmatch import *
	for i in range(2024, 10**10 + 1, 2024):
	    if fnmatch(str(i), '112?57*4'):
	        if sum(list(map(int, str(i)))) % 2 != 0: print(i, i // 2024)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие
символы:
символ «?» означает ровно одну произвольную цифру;
символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*475 соответствуют числа 123405 и 12300405.
Найдите первые пять чисел, больших 500 000, сумма делителей которых соответствует маске *77. Найденные числа выведите в порядке возрастания, справа от каждого запишите найденную сумму делителей.

	from fnmatch import *
	def f(n):
	    d = set()
	    for i in range(1, int(n**0.5) + 1):
	        if n % i == 0: d.add(i);d.add(n//i)
	    return d
	cnt = 0
	for i in range(500001, 10**10):
	    d = f(i)
	    if fnmatch(str(sum(d)), '*7?'): 
	        cnt += 1
	        print(i, sum(d))
	        if cnt == 5: break

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
символ «?» означает ровно одну произвольную цифру;
символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*475 соответствуют числа 123405 и 12300405.
Найдите все числа, меньшие 106, которые имеют ровно 24 делителя, соответствующих маске 4*, и максимальный делитель таких чисел, соответствующий маске.
В ответе укажите найденные числа в порядке возрастания, справа от каждого числа выведите его максимальный делитель, соответствующий маске.

	from fnmatch import *
	def f(n):
	    d = set()
	    for i in range(1, int(n**0.5) + 1):
	        if n % i == 0: d.add(i);d.add(n//i)
	    return d
	for i in range(1, 10**6):
	    d = f(i)
	    m = list(x for x in d if fnmatch(str(x), '4*'))
	    if len(m) == 24 and fnmatch(str(max(m)), '4*'):
	        print(i, max(m))  

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную чётную цифру;
    символ «*» означает любое нечётное число; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*4?5 соответствуют числа 123737465 и 123485.
Среди натуральных чисел, не превышающих 10¹⁰ , найдите 5 наибольших чисел, соответствующих маске ?136*, делящихся на 53191 без остатка.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце — соответствующие им результаты деления этих чисел на 53191.
Количество строк в таблице для ответа избыточно.

	for i in range(53191, 10**10 + 1, 53191):
	    s = list(map(int, str(i)))
	    if s[0] % 2 == 0 and s[1:4] == [1, 3, 6] and int(str(i)[4::]) % 2 != 0:
	        print(i, i // 53191)

МОЕ КРУТОЕ РЕШЕНИЕ!!
Назовём маской последовательность символов, в которой также могут встречаться такие символы:
    символ «?» означает ровно одну произвольную цифру
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность Например, в числе 1236547809 можно найти маску 3?5*0.
Найдите все числа, меньшие 108108, которые без остатка делятся на 1756 и в их написании можно найти маску 12*45*3. В качестве ответа приведите все найденные числа в порядке возрастания, справа от каждого найденного числа укажите результат его целочисленного деления на 1756.
Количество полей для ответа может быть избыточно.

	from fnmatch import *
	for i in range(1756, 10**8, 1756):
	    if fnmatch(str(i), '*12*45*3*'): print(i, i // 1756)

Назовём маской числа последовательность цифр, в которой также могут встречаться символы:
    «?» — означает ровно одну произвольную цифру
    «*» — означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность
Например, маске 123*4?5 соответствуют числа 123405 и 12300405. Найдите среди натуральных чисел, не превышающих 10101010, все числа, которые соответствуют маске 7?2*4??9? и делятся на 96437 без остатка.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором — соответствующие им результаты деления этих чисел на 96437.
Количество строк в таблице для ответа избыточно.

	from fnmatch import *
	for i in range(96437, 10**10 + 1, 96437): 
	    if fnmatch(str(i), '7?2*4??9?'): print(i, i // 96437)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «∗∗» означает любую последовательность цифр произвольной длины; в том числе «∗∗» может задавать и пустую последовательность. Например, маске 123∗4?5123∗4?5 соответствуют числа 123405123405 и 1230040512300405.
Среди натуральных чисел, не превышающих 10101010, найдите все числа, соответствующие маске 19?71∗32?19?71∗32? , делящиеся на 20242024 без остатка.
Для каждого найденного числа запишите это число и частное от деления этого числа на 2024.
Числа нужно записать в порядке возрастания, в одной строке — одно число и частное от деления найденного числа на 2024.

	from fnmatch import *
	cnt = 0
	for i in range(2024, 10**10, 2024):
	    if fnmatch(str(i), '19?71*32?'): 
	        cnt += 1
	        print(cnt, i, i // 2024, cnt)

СЛОЖНАЯ!!
Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность;
    символ «Ч» означает ровно одну четную цифру.
Например, маске 1Ч3*4?5 соответствуют числа 123405 и 16300425.
Найдите все натуральные числа, делящиеся нацело на 780 и 1323 не превышающие 3*10⁸, код которых соответствует маске «*Ч32??».
В ответе запишите найденные числа в десятичной системе счисления в порядке убывания, а справа от каждого числа — соответствующее частное от деления на 780.

	from fnmatch import *
	x = int(780 * 1323 / 3)
	cnt = 0
	for i in range(x, 3*10**8, x):
	    if fnmatch(str(i), '*?32??') and int(str(i)[-5]) % 2 == 0: 
	        cnt += 1
	        print(cnt, i, i // 780, cnt)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*4?5 соответствуют числа 123405 и 12300405.
Среди натуральных чисел, не превышающих 10**9, найдите первые семь чисел, подходящих под все перечисленные условия:
    кратны 124;
    соответствуют маске 1?3**5?;
    не соответствуют маскам 1*77*1 и 7*11*7.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце — соответствующие им результаты деления этих чисел на 124.

	from fnmatch import *
	cnt = 0
	for i in range(124, 10**9 + 1, 124):
	    if fnmatch(str(i), '1?3**5?') and fnmatch(str(i), '7*11*7') == 0 and fnmatch(str(i), '1*77*1') == 0:
	        cnt += 1
	        print(cnt, i, i//124, cnt)
	        if cnt == 7: break

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*4?5 соответствуют числа 123405 и 12300405.
Среди натуральных чисел, не превышающих 2⋅1082⋅108, найдите пять наибольших чисел, подходящих под все перечисленные условия:
    соответствуют маске ?2*4*0;
    не соответствуют маске 1*7*;
    делятся на число 42 без остатка.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце — соответствующие им результаты деления этих чисел на 42.

	from fnmatch import *
	cnt = 0
	for i in range(42, 2*10**8 + 1, 42):
	    if fnmatch(str(i), '?2*4*0') and fnmatch(str(i), '1*7*') == 0:
	        cnt += 1
	        print(cnt, i, i//42, cnt)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 13*4?5 соответствуют числа 13405 и 1300425.
Найдите все натуральные числа меньшие 10⁹, которые кратны 183 и соответствуют маске «??287*139».
В ответ в первом столбце перечислите все найденные числа в порядке возрастания, а во втором столбце соответственно результаты деления этих чисел на 183.

	from fnmatch import *
	for i in range(183, 10**9, 183):
	    if fnmatch(str(i), '??287*139'):
	        print(i, i // 183)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*4?5 соответствуют числа 123405 и 12300405.
Найдите все натуральные числа меньшие 10⁹, которые кратны 7977 и соответствуют маске «?1*23*92».
В ответ в первом столбце перечислите все найденные числа в порядке возрастания, а во втором столбце соответственно результаты деления этих чисел на 7977.

	from fnmatch import *
	for i in range(7977, 10**9, 7977):
	    if fnmatch(str(i), '?1*23*92'):
	        print(i, i // 7977)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*4?5 соответствуют числа 123405 и 12300405.
Среди натуральных чисел, не превышающих 10101010, найдите все числа, соответствующие маске 3?12?14*5, делящиеся на 1917 без остатка.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце — соответствующие им результаты деления этих чисел на 1917.
Количество строк в таблице для ответа избыточно.

	from fnmatch import *
	for i in range(1917, 10**10 + 1, 1917):
	    if fnmatch(str(i), '3?12?14*5'):
	        print(i, i//1917)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*4?5 соответствуют числа 123405 и 12300405.
Найдите все числа, меньшие 10¹², соответствующие маске 123?4*5679 и делящиеся без остатка на 4013. В качестве ответа приведите все найденные числа в порядке возрастания, справа от числа укажите результат целочисленного деления его на 4013.
Количество полей для ответа может быть избыточно.

	from fnmatch import *
	for i in range(4013, 10**12, 4013):
	    if fnmatch(str(i), '123?4*5679'): print(i, i//4013)

Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Например, маске 123*4?5 соответствуют числа 123405 и 12300405.
Среди натуральных чисел, не превышающих 10101010, найдите все числа, соответствующие маске 1?2157*4, делящиеся на 2024 без остатка.
В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце — соответствующие им результаты деления этих чисел на 2024.
Количество строк в таблице для ответа избыточно.

	from fnmatch import *
	cnt = 0
	for i in range(2024, 10**10 + 1, 2024):
	    if fnmatch(str(i), '1?2157*4'): 
	        cnt += 1
	        print(cnt, i, i//2024, cnt)

СЛОЖНАЯ!! - КРУТОЕ РЕШЕНИЕ ОТ МЕНЯ
Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    символ «?» означает ровно одну произвольную цифру;
    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
Среди натуральных чисел, не превышающих 10101010, найдите все числа, соответствующие маске 1*28?64 и делящиеся на 124 и ровно на 5 из всех двузначных чисел, соответствующих маске ?4, без остатка. В ответе запишите в первом столбце таблицы все найденные числа в порядке возрастания, а во втором столбце — соответствующие им частные от деления на 124.
Количество строк в таблице для ответа избыточно.

	from fnmatch import *
	cnt = 0
	for i in range(124, 10**10, 124):
	    if fnmatch(str(i), '1*28?64'):
	        l = list(x for x in range(14, 95, 10) if i % x == 0)
	        if len(l) == 5:
	            cnt += 1
	            print(cnt, i, i//124, cnt)

СЛОЖНАЯ!!
	
	Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
    	символ «?» означает ровно одну произвольную цифру;
    	символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
	Например, маске 123*4?5 соответствуют числа 123405 и 12300405.
	Среди натуральных чисел, не превышающих 10⁷, найдите все числа, удовлетворяющие маске 34?8*9 имеющие более 4 простых делителей (не равных самому числу и 1). В ответ в порядке возрастания выпишите подходящие числа и их наибольшие простые делители.

		from fnmatch import *
		def f(n):
		    d = set()
		    for i in range(2, int(n ** 0.5) + 1):
		        if n % i == 0: d.add(i);d.add(n//i)
		    return d
		cnt = 0
		for i in range(10**7 + 1):
		    if fnmatch(str(i), '34?8*9'):
		        l = list(x for x in f(i) if len(f(x)) == 0)
		        if len(l) > 4:
		            cnt += 1
		            print(cnt, i, max(l), cnt)

СЛОЖНАЯ!!

	Назовём маской числа последовательность цифр, в которой также могут встречаться следующие символы:
	    символ «?» означает ровно одну произвольную цифру;
	    символ «*» означает любую последовательность цифр произвольной длины; в том числе «*» может задавать и пустую последовательность.
	Например, маске 123*4?5 соответствуют числа 123405 и 12300405.
	Найдите все числа, меньшие 10⁶, которые имеют ровно 24 делителя, соответствующих маске 4*, и максимальный делитель таких чисел, соответствующий маске.
	В ответе укажите найденные числа в порядке возрастания, справа от каждого числа выведите его максимальный делитель, соответствующий маске.

		from fnmatch import *
		def f(n):
		    d = set()
		    for i in range(2, int(n ** 0.5) + 1):
		        if n % i == 0: d.add(i);d.add(n//i)
		    return d
		cnt = 0
		for i in range(10**6):
		    d = list(x for x in f(i) if fnmatch(str(x), '4*'))
		    if len(d) == 24 and fnmatch(str(max(f(i))), '4*'):
		        cnt += 1
		        print(cnt, i, max(d), cnt)