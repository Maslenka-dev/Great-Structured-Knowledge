ИСПОЛЬЗУЕМ СПИСОЧНЫЕ ВЫРЖАЖЕНИЯ (ГЕНЕРАТОРЫ). 

Откройте файл электронной таблицы, содержащей в каждой строке семь натуральных чисел. Определите количество строк, в которых второе по величине число больше предпоследнего по величине ровно на 20. 

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(int(x) for x in line.split())
	    if a[-2] - a[1] == 20: cnt += 1
	print(cnt)

Откройте файл электронной таблицы, содержащей в каждой строке четыре натуральных числа. Определите количество строк таблицы, содержащих числа, для которых выполнены оба условия:

    наибольшее из четырёх чисел меньше суммы трёх других;
    все четыре числа различны.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(int(x) for x in line.split())
	    if a[-1] < a[0] + a[1] + a[2]:
	        if len(set(a)) == 4: cnt += 1
	print(cnt)

СЛОЖНЫЙ!!
Откройте файл электронной таблицы, содержащей в каждой строке четыре натуральных числа. Определите количество строк таблицы, содержащих числа, для которых выполнены оба условия:

    сумма максимального и минимального значений кратна 3;
    четыре числа можно разбить на две пары так, чтобы разность значений в парах была одинаковой.

В ответе запишите только число.

	from itertools import *
	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    if (max(a) + min(a)) % 3 == 0:
	        for a1, a2, a3, a4 in permutations(a):
	            if a1 - a2 == a3 - a4:
	                cnt += 1
	                break
	print(cnt)

Откройте файл электронной таблицы, в каждой строке которой семь натуральных чисел. Определите количество строк таблицы с числами, для которых выполнены все условия:

    в строке не менее двух чётных и не менее двух нечётных чисел
    утроенная сумма нечётных чисел больше произведения чётных

В ответе запишите только число.

	from math import *
	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    a1 = list(x for x in a if x % 2 == 0)
	    a2 = list(x for x in a if x % 2 != 0)
	    if len(a1) >= 2 and len(a2) >= 2:
	        if (sum(a2) * 3) > prod(a1):
	            cnt += 1
	print(cnt)

СЛОЖНАЯ!!
Откройте файл электронной таблицы, содержащей в каждой строке шесть натуральных чисел. Определите количество строк таблицы, для которых выполняются все условия:

    в строке три числа повторяются ровно два раза;
    эти три числа образуют стороны прямоугольного треугольника.

В ответе запишите одно число.

	from math import *
	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split(','))
	    a1 = sorted(list(x for x in a if a.count(x) == 2))
	    if len(a1) == 6:
	        if a1[-1]**2 == ((a1[0]**2) + (a1[2]**2)): 
	            cnt += 1
	            print(a)
	print(cnt)

В файле в каждой строке содержится 5 целых чисел. Необходимо посчитать количество строк, в которых выполняется оба условия:

    ровно три числа заканчиваются цифрой 3;
    квадрат суммы положительных чисел меньше квадрата суммы отрицательных чисел.

В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    a1 = len(list(x for x in a if abs(x) % 10 == 3))
	    a2 = (x for x in a if x > 0)
	    a3 = (x for x in a if x < 0)
	    if (a1 == 3) and (sum(a2)**2 < sum(a3)**2): cnt += 1
	print(cnt)

Откройте файл электронной таблицы, содержащей в каждой строке четыре натуральных числа. Определите количество строк таблицы, для которых одновременно выполняются следующие условия:

    Максимальное число в кубе не менее удвоенного произведения трёх других чисел
    Все числа, содержащиеся в строке, больше 10

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split() if int(x) > 10))
	    if len(a) == 4:
	        if a[-1]**3 >= (a[0] * a[1] * a[2]) * 2: cnt += 1
	print(cnt)

Откройте файл электронной таблицы, содержащей в каждой строке семь натуральных чисел. Определите количество строк таблицы, для чисел которых выполнены оба условия:

    в строке есть ровно одно число, которое повторяется дважды, и пять чисел без повторений;
    произведение трёх наименьших среди неповторяющихся чисел строки больше квадрата повторяющегося числа.

В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    b = list(x for x in a if a.count(x) == 2)
	    c = sorted(list(x for x in a if a.count(x) == 1))
	    if len(b) == 2:
	        if len(c) >= 3:
	            if c[0] * c[1] * c[2] > b[0]**2: cnt += 1
	print(cnt)

Откройте файл электронной таблицы, содержащей в каждой строке пять натуральных чисел. Определите количество строк таблицы, содержащих числа, для которых выполнены оба условия:

    квадрат наибольшего значения больше произведения остальных чисел;
    сумма двух наибольших значений как минимум вдвое больше суммы остальных значений в строке.

В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split()))
	    if max(a)**2 > a[0] * a[1] * a[2] * a[3]:
	        if (a[-1] + a[-2]) / 2 >= a[0] + a[1] + a[2]: cnt += 1
	print(cnt)

Откройте файл электронной таблицы, содержащей в каждой строке шесть натуральных чисел. Определите количество строк таблицы, содержащих числа, для которых выполнены оба условия:

    в строке все числа различны;
    количество чётных чисел превышает количество нечётных чисел.

В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    b = list(x for x in a if a.count(x) == 1)
	    c = list(x for x in a if x % 2 == 0)
	    d = list(x for x in a if x % 2 != 0)
	    if len(b) == len(a):
	        if len(c) > len(d): cnt += 1
	print(cnt)

Откройте файл электронной таблицы, содержащей в каждой строке шесть натуральных чисел. Определите наибольший номер строки таблицы, для чисел которой выполнены оба условия:

    в строке есть одно число, которое повторяется трижды, остальные три числа различны;
    повторяющееся число строки больше, чем среднее арифметическое её неповторяющихся чисел.

В ответе запишите только число.

	cnt = 0
	r = []
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    cnt += 1
	    a = list(int(x) for x in line.split())
	    b = list(x for x in a if a.count(x) == 3)
	    c = list(x for x in a if a.count(x) == 1)
	    if len(b) == 3 and len(c) == 3:
	        if b[0] > sum(c) / len(c): 
	            r.append(cnt)
	print(max(r))

В файле электронной таблицы содержатся строки по 5 чисел в каждой. Найдите строки, соответствующие обоим условиям:

    В строке есть как положительные, так и отрицательные числа, но количество отрицательных чисел в строке больше чем положительных.
    Минимальное отрицательное число больше по модулю чем максимальное положительное.

В ответе укажите количество таких строк.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    b = list(x for x in a if x > 0)
	    c = list(x for x in a if x < 0)
	    if len(b) != 0 and len(c) != 0:
	        if len(c) > len(b):
	            if abs(min(c)) > max(b): cnt += 1
	print(cnt)

СЛОЖНАЯ!!!
Откройте файл электронной таблицы, содержащей в каждой строке пять натуральных чисел. Определите количество строк таблицы, содержащих числа, для которых выполняется ровно одно из двух условий:

    сумма всех нечётных значений больше суммы чётных значений (если чисел с определенным признаком чётности нет, то считаем сумму равной нулю);
    в строке одно число повторяется дважды, остальные числа различны.

В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    b = list(x for x in a if x % 2 == 0)
	    c = list(x for x in a if x % 2 != 0)
	    d = list(x for x in a if a.count(x) == 2)
	    f = list(x for x in a if a.count(x) == 1)
	    usl1 = False
	    usl2 = False
	    if len(b) > 0 and len(c) > 0:
	        if sum(c) > sum(b): usl1 = True
	    if len(b) == 0:
	        if sum(c) > 0: usl1 = True
	    if len(c) == 0:
	        if 0 > sum(b): usl1 = True
	    if len(d) == 2 and len(f) == 3: usl2 = True
	    if usl1 + usl2 == 1: cnt += 1
	print(cnt)	

Откройте файл электронной таблицы, содержащей в каждой строке пять натуральных чисел. Определите наибольший номер строки таблицы, для которой выполнены оба условия:

    в строке все числа различны;
    удвоенная сумма минимального и максимального чисел строки равна утроенной сумме трёх её оставшихся чисел.

В ответе запишите только число.

	cnt = 0
	r = []
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    cnt += 1
	    a = sorted(list(int(x) for x in line.split()))
	    b = list(x for x in a if a.count(x) == 1)
	    if len(b) == len(a):
	        if (max(a) + min(a))*2 == sum(a[1:-1])*3: r.append(cnt)
	print(max(r))

СЛОЖНАЯ ПИЗДЕЦ!!
В файле электронной таблицы в каждой строке содержатся шесть натуральных чисел. Определите количество строк таблицы, для которых выполнены оба условия:

    в строке хотя бы одно число повторяется дважды (ровно 2 раза);
    каждое из повторяющихся дважды (ровно 2 раза) чисел превышает каждое неповторяющееся.

	c = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = [int(x) for x in line.split()]
	    p2 = [x for x in a if a.count(x) == 2]
	    np = [x for x in a if a.count(x) == 1]
	    if len(p2) >= 2:
	        p2 = sorted(set(p2))
	        np = sorted(set(np))
	        if len(np) != 0:
	            if min(p2) > max(np):
	                print(p2, np)
	                c += 1
	        if len(np) == 0: 
	            if min(p2) > 0:
	                print(p2, np)
	                c += 1
	print(c)

Откройте файл электронной таблицы, содержащей в каждой строке четыре натуральных числа. Определите количество строк таблицы, для которых одновременно выполняются следующие условия:

    Удвоенный квадрат минимального из чисел, содержащихся в строке, больше, чем произведение двух других чисел, ни одно из которых не равно максимальному числу
    В строке содержится хотя бы два одинаковых числа

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split()))
	    if len(set(a)) <= 3:
	        if a[1] != a[-1] and a[2] != a[-1]:
	            if (a[0]**2)*2 > a[1] * a[2]: cnt += 1
	print(cnt)

Откройте файл электронной таблицы, содержащей в каждой строке семь натуральных чисел. Определите сумму всех чисел в строке таблицы с наименьшим номером, для чисел которой выполнены оба условия:

    в строке есть два числа, каждое из которых повторяется дважды, остальные три числа различны;
    максимальное число строки не повторяется.

В ответе запишите только число.

	r = []
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    b = list(x for x in a if a.count(x) == 2)
	    c = list(x for x in a if a.count(x) == 1)
	    if a.count(max(a)) == 1:
	        if len(b) == 4 and len(c) == 3:
	            print(sum(a))
	            break

Откройте файл электронной таблицы, содержащей в каждой строке три натуральных числа.
Определите количество строк таблицы, в которых медианное число чётное.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split()))
	    if a[1] % 2 == 0: cnt += 1
	print(cnt)

Откройте файл электронной таблицы содержащий в каждой строке восемь натуральных чисел.
Определите сумму всех чисел в строке с наименьшим номером, для чисел которой выполнены оба условия:

    В строке есть два числа, каждое из которых повторяется трижды, остальные два числа различны.
    Минимальное число в строке не повторяется.

В ответе запишите только сумму чисел в соответствующей строке.

СЛОЖНАЯ!!
Дед Мороз составляет списки подарков для детей. Откройте файл электронной таблицы, в каждой строке которой шесть натуральных чисел. Каждое означает количество определённого вида подарков (конфеты, мандарины, шоколадки, игрушки, книги, наборы для творчества). Определите количество строк таблицы с числами, для которых работают не менее двух из трёх условий:

    в строке одно число повторяется трижды, остальные различны
    чётных чисел больше, чем нечётных
    сумма двух наибольших значений больше, чем в два раза превышает сумму остальных значений

В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    f1 = False
	    f2 = False
	    f3 = False
	    a = sorted(list(int(x) for x in line.split()))
	    b = set(list(x for x in a if a.count(x) == 3))
	    c = list(x for x in a if a.count(x) == 1)
	    d = list(x for x in a if x % 2 == 0)
	    e = list(x for x in a if x % 2 != 0)
	    if len(b) == 1 and len(c) == 3: f1 = True
	    if len(d) > len(e): f2 = True
	    if (a[-1] + a[-2]) / 2 > (a[0] + a[1] + a[2] + a[3]): f3 = True
	    if f1 + f2 + f3 >= 2: cnt += 1
	print(cnt)

В файле электронной таблицы в каждой строке содержатся четыре натуральных числа. Сколько среди них строк, в которых квадрат суммы максимального и минимального чисел в строке больше суммы квадратов двух оставшихся?

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split()))
	    if (a[-1] + a[0])**2 > a[1]**2 + a[2]**2: cnt += 1
	print(cnt)

СЛОЖНАЯ!!
Откройте файл электронной таблицы, содержащей в каждой строке четыре натуральных числа.
Определите количество строк таблицы, содержащих числа, для которых выполнены оба условия:

– наибольшее из четырёх чисел меньше суммы трёх других;
– четыре числа можно разбить на две пары чисел с равными суммами.

В ответе запишите только число.

	from itertools import *
	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split()))
	    if a[-1] < a[0] + a[1] + a[2]:
	        for x1, x2, x3, x4 in permutations(a):
	            if x1 + x2 == x3 + x4:
	                cnt += 1
	                break
	print(cnt)

	ИЛИ

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split()))
	    if a[-1] < a[0] + a[1] + a[2]:
	        if a[-1] + a[0] == a[1] + a[2]: cnt += 1
	print(cnt)

СЛОЖНАЯ!!
Откройте файл электронной таблицы, содержащей в каждой строке семь натуральных чисел. Определите количество строк таблицы, содержащих числа, для чисел которых выполнены оба условия:
– в строке есть одно число, которое повторяется трижды, одно число, которое повторяется дважды и два числа, которые различны;
– cумма различных повторяющихся чисел не меньше суммы неповторяющихся чисел.
В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = list(int(x) for x in line.split())
	    b = list(x for x in a if a.count(x) == 3)
	    c = list(x for x in a if a.count(x) == 2)
	    d = list(x for x in a if a.count(x) == 1)
	    if len(set(b)) == 1 and len(set(c)) == 1 and len(d) == 2:
	        if b[0] + c[0] >= sum(d): cnt += 1
	print(cnt)

СЛОЖНАЯ!!
Откройте файл электронной таблицы, содержащей в каждой строке шесть натуральных чисел. Определите наибольший номер строки таблицы, для чисел которой выполнены оба условия:
– в строке находится равное количество чётных и нечётных чисел;
– шесть чисел можно разбить на три пары чисел с равными суммами.
В ответе запишите только число. 

	from itertools import *
	cnt = 0
	r = []
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    cnt += 1
	    a = sorted(list(int(x) for x in line.split()))
	    b = list(x for x in a if x % 2 == 0)
	    c = list(x for x in a if x % 2 != 0)
	    if len(b) == len(c):
	        for x1, x2, x3, x4, x5, x6 in permutations(a):
	            if x1 + x2 == x3 + x4 == x5 + x6:
	                r.append(cnt)
	                break
	print(max(r))

	ИЛИ

	cnt = 0
	r = []
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    cnt += 1
	    a = sorted(list(int(x) for x in line.split()))
	    b = list(x for x in a if x % 2 == 0)
	    c = list(x for x in a if x % 2 != 0)
	    if len(b) == len(c):
	        if a[-1] + a[0] == a[-2] + a[1] == a[-3] + a[2]: r.append(cnt)   
	print(max(r))
    
СЛОЖНАЯ ПИЗДЕЦ!!
Откройте файл электронной таблицы, содержащей в каждой строке семь натуральных чисел. Определите сумму номеров строк таблицы, для чисел которых выполнены оба условия:
– в строке чётность всех соседних чисел различна;
– утроенная сумма неповторяющихся чисел строки не меньше произведения повторяющихся чисел.
В ответе запишите только число.
Примечание. Если в строке повторяющихся чисел нет, произведение следует принять равным нулю.

	from math import *
	cnt = 0
	r = []
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    cnt += 1
	    a = list(int(x) for x in line.split())
	    b = list(x for x in a if a.count(x) == 1)
	    c = list(x for x in a if a.count(x) > 1)
	    if all((a[i] % 2 )!= (a[i+1] % 2) for i in range(len(a) - 1)): 
	        if len(c) != 0:
	            if sum(b)*3 >= prod(c): r.append(cnt)
	        else:
	            if sum(b)*3 >= 0: r.append(cnt)
	print(sum(r))

СЛОЖНАЯ!!
Откройте файл электронной таблицы, содержащей в каждой строке семь натуральных чисел. Определите количество строк таблицы, для чисел которых выполняется только одно из условий:
— произведение наименьшего и наибольшего чисел строки меньше утроенной суммы оставшихся чисел;
— сумма неповторяющихся чисел строки не больше суммы повторяющихся чисел.
В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split()))
	    b = list(x for x in a if a.count(x) == 1)
	    c = list(x for x in a if a.count(x) > 1)
	    if (a[-1] * a[0] < (a[1] + a[2] + a[3] + a[4] + a[5])*3) + (sum(b) <= sum(c)) == 1: cnt += 1
	print(cnt)

СЛОЖНАЯ!!
Откройте файл электронной таблицы, содержащей в каждой строке семь натуральных чисел. Определите сумму всех чисел в строке таблицы с наименьшим номером, для чисел которой выполнены оба условия:
— в строке есть два числа, каждое из которых повторяется дважды, остальные три числа различны;
— сумма всех повторяющихся чисел строки не больше суммы всех нечётных чисел.
В ответе запишите только число.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    cnt += 1
	    a = sorted(list(int(x) for x in line.split()))
	    b = list(x for x in a if a.count(x) == 2)
	    c = list(x for x in a if a.count(x) == 1)
	    d = list(x for x in a if x % 2 != 0)
	    if len(set(b)) == 2 and len(c) == 3:
	        if sum(b) <= sum(d): 
	            print(cnt, sum(a))
	            break

СЛОЖНАЯ!!
 Откройте файл электронной таблицы, содержащей в каждой строке шесть неотрицательных целых чисел. Определите количество строк таблицы, содержащих числа, для которых выполнено только одно из условий:
– в строке только одно число повторяется дважды, а остальные не повторяются;
– в строке среднее арифметическое чётных чисел отличается от среднего арифметического нечётных чисел более чем на 50.
В ответе запишите только число.
Примечание. Среднее арифметическое для 0 чисел принять равным нулю.

	cnt = 0
	for line in open(r'C:\Users\User\Downloads\9\9xx.txt'):
	    a = sorted(list(int(x) for x in line.split()))
	    b = list(x for x in a if a.count(x) == 2)
	    c = list(x for x in a if a.count(x) == 1)
	    d = list(x for x in a if x % 2 == 0)
	    if len(d) == 0: srard = 0
	    else: srard = sum(d) / len(d)
	    f = list(x for x in a if x % 2 != 0)
	    if len(f) == 0: srarf = 0
	    else: srarf = sum(f) / len(f)
	    if (len(b) == 2 and len(c) == 4) + (abs(srard - srarf) > 50) == 1: 
	        cnt += 1
	print(cnt)